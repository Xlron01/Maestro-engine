# Engine Principles — مبادئ المحرك الثابتة

هذه الوثيقة تمثل "دستور" المشروع — قرارات لا يفترض أن تتغير إلا نادرًا جدًا. أي تعارض بين قرار جديد وهذه المبادئ يعني أن القرار الجديد خاطئ، لا العكس.

## السياق العام

البداية كانت مود لـ HoI4 (Millennium Dawn). بعد نقاش طويل، اتضح إن الهدف الحقيقي أكبر: **بناء محرك متخصص لألعاب Grand Strategy** — مش محرك عام زي Unity/Unreal (مستحيل واقعيًا لفريق صغير)، لكن زي **Clausewitz بتاع Paradox**: محرك مبني لنوع لعبة واحد، قابل لإعادة الاستخدام عبر سلسلة ألعاب مختلفة.

**الهدف مش AI حقيقي (LLM/ML)** — الهدف طبقة أذكى من الموجود حاليًا باستخدام معادلات وزن بسيطة (Utility AI) بدل قرارات عشوائية، من غير أي تضحية في الأداء، بل تحسينه.

**خلفية المستخدم**: بدون خبرة برمجة ألعاب سابقة — عامل حاسم في كل قرار تنفيذي.

---

## 1) القاعدة المعمارية الأهم: تكلفة الحساب محكومة بالصلة لا بالحجم

> أي حساب لأي كيان، تكلفته لازم تكون **O(عدد الروابط ذات الصلة)**، **مش O(كل الدول)** أبدًا تحت أي ظرف.

نظام واحد بس بيكسر القاعدة دي (بيعمل O(n²)) كفيل يرجّع كل مشاكل الأداء حتى لو باقي الأنظمة ملتزمة 100%. الخطر الحقيقي مش في التصميم، في **الانضباط التنفيذي طويل المدى**.

## 2) الفصل الصارم بين Engine و Content

المعيار الفاصل: **لو غيّرت نوع اللعبة بالكامل (من Grand Strategy تاريخي لمحاكاة فضائية)، هل الكود ده هيفضل شغال زي ما هو؟**
- أيوه → Engine
- لأ (محتاج إعادة كتابة) → Gameplay/Content

قاعدة تقنية تفرض الفصل تلقائيًا: **المحرك ممنوع يعرف أسماء دومين** زي `Coup`, `Population`, `Ideology`, `Parliament`, `Democracy`. لو لقيت كود جوه المحرك بيكتب `if entity.type == "coup"` أو بيحمل "افتراضات" عن معنى مفاهيم اللعبة (زي "ديمقراطية = برلمان قوي")، ده تسريب. المحرك يتعامل مع أسماء عامة بس: `Entity`, `Component`, `Variable`, `Event`, `Node`.

هذا ينطبق أيضًا على أي "استنتاج ذكي" (Infer): المحرك ينفذ فقط قواعد يحددها ملف بيانات اللعبة (`Migration Rules` مكتوبة صراحة)، ولا يفترض علاقات بين مفاهيم اللعبة بنفسه أبدًا.

## 3) Determinism (الحتمية) — غير قابل للتفاوض

نفس المدخلات = نفس النتيجة، دائمًا. هذا ضروري لأمرين:
- **Debugging**: بدون حتمية، لا يمكن معرفة سبب أي نتيجة غريبة أو تكرارها لتشخيصها.
- **Scenario Tests** (انظر وثيقة Architecture): لا تفيد إلا مع نتائج قابلة للتكرار.

القرار: **Seeded RNG** لكل عشوائية، مغلّف داخل **Simulation Context** (Seed + Current Date + Current Tick + Version + Rule Set + Mod List + Scenario Hash) — أي حدث عشوائي يعتمد على السياق الكامل، لا على Seed وحده.

## 4) Data-Driven بالكامل (Plugin System منذ اليوم الأول)

كل محتوى اللعبة (خرائط، دول، معادلات، قواعد) = ملفات بيانات خارجية (JSON/Resources)، وليس كود مدمج. المحرك يقرأ من `data/` — هذا بحد ذاته **Plugin System**، وليس حاجة إضافية نبنيها لاحقًا. لا حاجة لـ "Modding API" رسمي منفصل — الـ API يتولّد من نظام الـ Plugin نفسه عند الحاجة.

## 5) Data-Oriented Design (ECS)

تخزين كل خاصية في array منفصل (Structure of Arrays) بدل object لكل كيان — أداء cache-friendly أعلى بكثير من الأسلوب التقليدي (كل كيان object منفصل بخصائصه بداخله). هذا أساس التعامل مع آلاف الكيانات بلا Lag.

## 6) Serialization Layer موحّدة (ليست "Save" فقط)

نفس آلية التحويل من/إلى تمثيل مخزّن تُستخدم لـ: Save، تحميل History/Scenarios، Testing، وأي احتياج مستقبلي (Replay, Networking, Debug Snapshot). لا نبني نظامين منفصلين لهذه الاستخدامات.

## 7) بيانات > كود لأي قيمة قابلة للتوازن

أي معامل رقمي (أوزان، عتبات، معدلات) في أي معادلة — لا يوضع داخل الكود أبدًا. يوضع في ملفات بيانات منفصلة (Rules Database مقسّمة بالدومين: `economy.json`, `politics.json`, `military.json`...) قابلة للتعديل والتوازن بدون إعادة بناء اللعبة.

## 8) القاعدة الفاصلة بين "بيانات" و"ذكاء"

> لو قدرت تحسب القيمة مرة واحدة وتخزنها (lookup)، فهي **بيانات**، مش **ذكاء**.

هذا الفرق بين مشروع أسابيع ومشروع سنين بحثية. رُفضت صراحة: MCTS، Reinforcement Learning، أي محاكاة سيناريوهات مستقبلية حية. كل "ذكاء" في هذا المشروع = معادلات وزن بسيطة (Utility AI) تُحسب من بيانات موجودة أصلًا.

## 9) استبعادات صريحة (قرارات نهائية موثقة)

- **لا نماذج 3D للوحدات/المباني** — كل تمثيل بصري = أيقونة 2D. هذا امتداد مباشر لمبدأ التمثيل المجمّع بدل الفردي.
- **لا محرك عام شامل الأنواع** (Unity/Unreal-style) — الهدف محرك متخصص فقط.
- **لا فلسفة Live-Service/Mobile** (زي Lords Mobile) — لا سيرفرات، لا اقتصاد دفع، لا بساطة إدمانية. الهدف عمق محاكاتي أقرب لـ HoI4/Victoria.
- **لا AI حقيقي (LLM/ML) داخل حلقة المحاكاة الحية.**

## 10) معيار نجاح المحرك (Developer Experience)

> إذا كان المحرك ممتازًا تقنيًا، لكن إضافة دولة جديدة تحتاج 40 ملفًا يدويًا، فالتصميم فشل.

المعيار الملموس: إضافة دولة جديدة = `Country Folder → country.json → flag.png → leader.json` وانتهى. يُختبر هذا المعيار فعليًا بعد أول Prototype، ويُعاد التبسيط إذا لم يتحقق.

---

## الترتيب التنظيمي للوثائق

هذه الوثيقة (Engine Principles) + وثيقة Architecture + وثيقة Gameplay Framework — الثلاثة منفصلة عمدًا لتفادي تحوّل وثيقة واحدة ضخمة لمشروع غير قابل للرجوع إليه. كل قرار جديد يُصنّف أولًا: هل هو مبدأ ثابت (هنا)، تفصيل معماري قابل للتطور (Architecture)، أم محتوى لعبة (Gameplay Framework)؟
