# 26 — T5-C: Storm Root-Cause & Measured-Bottleneck-Only Fix

> **الحالة:** COMPLETE (PROVISIONAL — verdicts لكل مرشّح؛ القرار المعماري النهائي/Migration قرار المالك حصريًا)
> **السلسلة:** T5-P0 (لوحظ العنق الزجاجي: HARD_STOP يوم 30) → T5-B (بدائل المجدول؛ Bucket=FULL PASS) → **T5-C (هذه الوثيقة): لماذا ما زالت العاصفة بطيئة جدًا حتى بعد Bucket؟ → نصلح فقط ما يقيسه E1**
> **النطاق:** مسار تجريبي معزول — **صفر تعديل على ملفات النواة التسعة** (الملفات الجديدة كلها في `scripts/experimental/` + عدّاء harness). حمل حقيقي حصرًا: `data/scenarios/t5_p0` (10,000 دولة، 40,002 jobs، seed=12345).
> **البوابات المجمّدة قبل التشغيل:** `SEM_HASH=bcd2763c…c2b4` · `SEQ_HASH=795365ba…b89d` · counters `events=30104 coup=6000 exposure=0` (تشغيلات 90 يومًا). أي اختلاف = FAIL.

---

## 0) الخلاصة التنفيذية

| المكوّن/المرشّح | النتيجة | الحكم |
|---|---|---|
| **الجذر (E1)** | **99.2–99.5% من زمن العاصفة = `EventQueue.push_event` يعيد فرز المصفوفة كاملة مع كل دفعة** (EventQueue.gd:18) عبر 10K دفعة/عاصفة بمصفوفة تكبر حتى 10,001 عنصر | مثبت بالقياس |
| **فاتورة القياس (E0)** | الـSchedulerProbe الثقيل: current +155s (+28%)، bucket +788s (+125%) — وأرقام T5-B العاصفية لا تتكوّر منزوعة القياس | مثبت |
| **C1 — BatchedEventQueue** | عواصف −99.8% (0.36–0.48s مقابل 191/180/176s) · SEM/SEQ/counters **bitwise PASS** | **PASS** |
| **C2 — SlicedRunner** | تكافؤ ظل bitwise ✓ · توزيع wall-clock يلتزم 30s عند K≤1000 (max frame 24.4s = 81.5%) · **لا يقلّص الزمن الكلي للعاصفة** | **PASS (محور الـframe فقط)** |
| **E4 — day-shift (ضابط سلبي)** | SEQ ينحرف فورًا في كل الصيغ؛ SEM ينحرف فورًا في spread؛ m29/m31 تطابق SEM عرضي خلال 35 يومًا فقط | **FAIL — مرفوض** |
| **C3 — تحسينات تنفيذ** | غير مفعّلة: كلفة التنفيذ الفعلية ≈ 0.3% من العاصفة (أقل من عتبة 15%) | غير مفعّلة بقرار القياس |

**الإجابة على سؤال T5-C:** العاصفة لم تكن مشكلة جدولة أصلًا بعد إزالة مسح 40K (وهو 0.005–0.3% من العاصفة) — العاصفة هي **طابور الأحداث**. Bucket كان تحسينًا صحيحًا للـquiet day (−98%) لكنه لم يعالج العاصفة إطلاقًا (وإسهامه فيها لا يتجاوز 0.3%).

---

## 1) E0 — الأساس الرسمي الجديد (نفس البيئة، نفس الحملة)

المصفوفة: {current, bucket} × {probe ON/OFF} × 90 يومًا — أربعة تشغيلات متتالية في نفس الجلسة. البوابات bitwise **PASS في الأربعة**.

| الوضع | d30 | d60 | d90 | الإجمالي | quiet p50 | mem@d90 |
|---|---|---|---|---|---|---|
| **OSB = Current + probe OFF** (الأساس الرسمي) | **191.15s** | **180.34s** | **176.01s** | **548.28s** | 8.661ms | 95.4MB |
| **BSB = Bucket + probe OFF** | 190.92s | 203.94s | 236.27s | 631.16s | **200µs** | 120.6MB |
| Current + probe ON | 245.64s | 215.78s | 240.91s | 703.30s | 10.885ms | 91.8MB |
| Bucket + probe ON | 547.68s | 493.92s | 377.98s | 1419.64s | 349µs | 116.9MB |

### 1-a) فاتورة القياس (H2 محسومة كميًا)
- current: +155.0s إجمالًا (+28.3%) · bucket: +788.5s إجمالًا (+124.9%!). الـheavy probe يبني سلسلة seq لكل واجب مستحق عبر `+=` (O(K²)) — ممنوع على مقاييس العاصفة.
- **إعادة تقييم أرقام T5-B (تاريخيًا فقط):** ميزة bucket العاصفية المعلنة (−38…−41%) **لا تتكوّر** على الأساس منزوع القياس بنفس البيئة: bucket≈current يوم d30، وأسوأ منه في d60/d90. ما يتكوّر هو فوز الأيام الهادئة (77×). حكم T5-B الرسمي لا يُلغى من هنا — قرار المالك — لكن مرجعيته الزمنية تنتهي عند E0.
- تباين بيئي بين الجلسات ±30% موثق (T5-P0 run06: 147.4s لنفس المسار مقابل 191.2s هنا) — لذلك تُحسب كل نسب T5-C من OSB/BSB بنفس الحملة فقط.

## 2) E1 — تفكيك العاصفة (البوابة الحاكمة)

أدوات القياس كلها خارج النواة (`event_queue_probe.gd` + `scheduler_probe_lite.gd` + لفّ handlers على مستوى العدّاء). كلفة القياس داخل التكة تحليليًا <0.05% من العاصفة (لا مؤقّتات ثقيلة ولا سلاسل لكل واجب). فروقات الإجماليات بين العمليات = تباين بيئة (موثق).

### شجرة التفكيك — d30
```
Storm d30 (Current, decompose) ............ 209.83s
├─ Scheduler scan ...................... 0.012s (0.005%)
├─ EventQueue.push_event (الفرز داخل الدفع) 208.82s (99.5%)  ← الجذر
├─ Execution (أجسام handlers، بلا الدفع) ... ≈0.35s (0.17%)
│    military_readiness 10,000 نداء = 209.16s شاملة الدفع
│    (DS evaluate+apply داخلها = 0.147s فقط)
├─ EventQueue pops ...................... 0.043s (0.02%)
└─ Residual ............................. <0.01s
Storm d30 (Bucket, decompose) ............. 227.55s
├─ Scheduler drain+sort ................. 0.709s (0.3%)
└─ EventQueue.push_event ................ 225.75s (99.2%)  ← نفس الجذر
```
- d90 مطابق النمط (eq_push = 99.5%/99.2%) مع انضمام gdp_update (52ms).
- **التشخيص الحاسم:** `push_max_arr=10001, push_avg_arr=5001.5, ties_consecutive=10000` — كل أحداث military_readiness تُدفع بنفس اللحظة (time=t) فتتضخم كلفة الفرز تراكميًا: Σ i·log(i) على 10K دفعة.
- البوابات على تشغيلي التفكيك: PASS bitwise · `EVENT_SEQ_HASH=9e35a648…` (baseline الأحداث) متطابق بين المجدولين.

### جدول القرار المجمد — المطبَّق
| المكوّن | نصيبه من العاصفة المصححة | القرار |
|---|---|---|
| EventQueue (push+pop) | 99.2–99.5% | ≥15% ⇒ **C1 مفعّلة** |
| Execution/dispatch | ≈0.3% | <15% ⇒ C3 غير مفعّلة |
| فاتورة Probe | +28%/+125% | ≥15% ⇒ موثقة (§1-a) — لا مرشح محرك منها |
| Residual | <0.5% | لا حاجة لتعميق التفكيك |

## 3) C1 — BatchedEventQueue (المرشّح المفعّل)

`scripts/experimental/batched_event_queue.gd` — حقن post-init عبر swap (نمط T5-B). **بنيوي لا دلالي**: push = append فقط؛ فرز واحد lazy عند أول قراءة بعد أي دفعة (نفس مقارن baseline حرفيًا)؛ صرف بمؤشر رأس بدل `pop_front` O(E)؛ ضغط قبل أي فرز بعد الصرف الجزئي.

**بوابة الصحة (90 يومًا، على current وبucket):** SEM/SEQ/counters **bitwise PASS** في الحالتين.
**انحراف معلن وموثق:** `EVENT_SEQ_HASH` تغيّر من `9e35a648…` إلى `afbef37e…` — ترتيب pop بين الأحداث المتساوية الزمن (التي كانت أصلًا ناتج فرز غير مستقر في baseline). أثر ذلك على SEM صفر لأن أحداث العاصفة المرتبطة كلها `Economic_Investment` → `evt_noop` (قيست: 10,000 نداء = 3.5ms)، وخصم stability بclamp تبادلي. البوابة الرسمية (SEM/SEQ/counters) هي الحاكمة ومرّت.

| الوضع | d30 | d60 | d90 | الإجمالي | Δ مقابل الأساس |
|---|---|---|---|---|---|
| Current + C1 | **0.361s** | **0.352s** | **0.480s** | **2.069s** | **−99.8% لكل عاصفة / −99.6% إجمالًا** |
| Bucket + C1 | 1.051s | 1.073s | 1.010s | 3.175s | −99.5% لكل عاصفة |

- 62 فرزًا فقط في 90 يومًا (إجماليها 125ms) بدل 30,100 فرز تراكمي.
- **Verdict C1: PASS** — بوابات bitwise + خفض ≥15% (تحقق 99.8%).
- ملاحظة صادقة: peak **العمل الدلالي** لكل تكة لم ينقص (نفس 30K وظيفة + 10K حدث) — ما انخفض هو **كلفة تنفيذ نفس العمل** (إزالة العمل المكرر في الفرز، لا إزالة عمل).

## 4) C2 — SlicedRunner (توزيع wall-clock داخل التكة)

`scripts/experimental/sliced_runner.gd` — منفّذ ظل يكرر run_step بندًا بندًا مع checkpoint كل K عنصر عمل، وزمن المحاكاة متجمد حتى اكتمال عمل اليوم.

**بوابة التكافؤ (K=∞، ظل بإطار واحد/يوم، 90 يومًا):** SEM/SEQ/counters **bitwise PASS** — النسخة الظل مطابقة للإنتاج.
**التشغيل المقطّع (current):**

| K | d30 max frame | d60 max frame | d90 max frame | Semantic tick d30 | بوابات |
|---|---|---|---|---|---|
| 5000 | 82.0s (**273%** من 30s) | 57.5s (192%) | 49.7s (166%) | 233.3s | PASS |
| **1000** | **24.4s (81.5% ✓)** | **20.0s (66.6% ✓)** | **11.4s (38.2% ✓)** | 240.1s | PASS |

### القاعدة الإلزامية: Semantic tick duration ≠ Wall-clock frame duration — يُسجلان منفصلين
- SlicedRunner **لا يسرّع المحاكاة**: الزمن الدلالي للعاصفة بقي ~176–240s في كل الأوضاع؛ ما فعله هو توزيعه على 41–51 frame لا يتجاوز أكبرها 24.4s عند K=1000.
- **إن كان الـ30s قيد استجابة frame/tick** ⇒ C2 حل compliant (عند K≤1000).
- **إن كان الـ30s زمن تنفيذ فعلي لخطوة المحاكاة** ⇒ C2 وزّع المشكلة لا حلّها — ويُنص بهذا صراحة.
- **مع C1 تسقط الحاجة إلى C2 لهذا الحمل**: التكة الدلالية كلها تصبح 0.36–0.48s ≪ 30s بلا أي تقطيع.
- درس تقني: التقطيع بعدد موحد ≠ تقطيع بزمن موحد — كلفة الدفع فائقة الخطية فتتركز في الإطارات الأخيرة (لهذا فشل K=5000 ونجح K=1000).

**Verdict C2: PASS على محور الامتثال للـframe فقط** (تكافؤ bitwise + max frame < 30s عند K≤1000)، مع النص الحرفي أعلاه: ليس حلًا لمدة العاصفة.

## 5) E4 — الضابط السلبي: إعادة توزيع calendar-time (بند 5 من أمر المالك)

35 يومًا، تعديل `next_check` لفئة population_update فقط (harness-only):

| الصيغة | SEM vs baseline35 | SEQ vs baseline35 |
|---|---|---|
| baseline35 | `42c9fa27…` (المرجع) | `cc7f7548…` (المرجع) |
| m29 (يوم 29) | **متطابق عرضيًا** | **منحرف** (`797d8582…`) |
| m31 (يوم 31) | **متطابق عرضيًا** | **منحرف** (`03092b72…`) |
| spread (نثر 1..30) | **منحرف** (`5080d6cd…`) | **منحرف** (`a52793a9…`) |

**التحليل الصادق:**
1. **SEQ ينحرف فورًا وبلا استثناء** — تسلسل التنفيذ (بوابة مجمّدة) تغيّر بكل صيغة ⇒ **day-shift FAIL رسميًا**.
2. **spread يكسر SEM فورًا**: النثر يغيّر عدد مرات الإطلاق داخل النافذة (بعض الدول أطلقت مرتين بحلول يوم 31 مقابل مرة في baseline) — population مركّبة فتختلف القيمة النهائية. هذا هو البرهان التجريبي المطلوب: تغيير التوقيت يغيّر مسار الحالة.
3. تطابق SEM في m29/m31 **خدعة نطاق** لا برهان تكافؤ: population حاليًا حقل ورقي لا يقرأه أي handler (مثبت بالقياس: DS لا يقرأ population وإلا انحرف m31)، وعدد الإطلاقات محفوظ داخل 35 يومًا. ينهار التطابق عند m31 بعد يوم 60 (2 إطلاقات مقابل 3 بحلول يوم 90) ومع أي handler مستقبلي يقرأ population أو أي حالة عابرة للزمن.
4. **Verdict E4: FAIL (مرفوض)** — التوزيع على أيام مجاورة يغيّر الدلالة الزمنية؛ التوزيع الوحيد الحافظ للدلالة هو wall-clock داخل التكة (C2).

## 6) مصفوفة المقارنة النهائية (كلها نفس الحملة والبيئة)

Semantic tick duration (s) / Wall-clock frame max (s):

| الوضع | d30 | d60 | d90 | إجمالي 90ي | quiet p50 | المتبقي من 30s (tick) | المتبقي من 30s (frame) |
|---|---|---|---|---|---|---|---|
| OSB (Current) | 191.2 | 180.3 | 176.0 | 548.3 | 8.66ms | **637%** (مخالف) | — |
| BSB (Bucket) | 190.9 | 203.9 | 236.3 | 631.2 | **0.20ms** | 636–788% (مخالف) | — |
| OSB + C1 | **0.361** | **0.352** | **0.480** | **2.069** | 9.70ms | **1.2%** ✓ | لا حاجة للتقطيع |
| BSB + C1 | 1.051 | 1.073 | 1.010 | 3.175 | 0.19ms | 3.4% ✓ | لا حاجة للتقطيع |
| OSB + C2 (K=1000) | 240.1 / 24.4 | 262.3 / 20.0 | 202.3 / 11.4 | 705.6 | 10.08ms | 800% (tick كما هو) | **81.5% ✓** |

**إجابات بنود الأمر صراحة:**
- هل انخفض peak work per tick؟ العمل الدلالي لا يتغير (بنيوي)؛ **الكلفة** انخفضت −99.8% بـC1. بـC2 انخفض peak work **لكل frame** فقط.
- هل انخفضت مدة storm؟ نعم بـC1: 191.2s → 0.361s (−99.8%). لا مع C2 (وزعها فقط).
- كم بقي من الـ30s hard stop؟ مع C1: التكة تستخدم 1.2% من السقف. بـC2 فقط: الـframe يستخدم ≤81.5% عند K=1000، والـtick ما زال 800%.
- تكلفة كل مكوّن؟ §2 — push_event 99.2–99.5%، المجدول ≤0.3%، التنفيذ 0.3%، الباقي <0.5%.

## 7) Verdicts والتوصية (بلا قرار migration)

- **C1 = PASS** (بوابات bitwise + −99.8% عاصفة) · **C2 = PASS محور الـframe فقط** · **E4/day-shift = FAIL مرفوض** · **C3 غير مفعّلة** (هدفها غير موجود قياسًا).
- **توصية الخطوة التالية (قرار المالك):**
  1. إن رغب المالك في مسار تبني: مهمة جديدة (T5-D مقترحة) لترحيل نمط BatchedEventQueue إلى `EventQueue.gd` — ملف نواة واحد ضمن الميزانية، بشرطين: (أ) حسم سياسة دلالة روابط الأحداث المتساوية الزمن (اليوم: فرز غير مستقر عمليًا؛ المقترح: تثبيت (time, seq) كترتيب كلي معلن)، (ب) دراسة الذاكرة (C1 ≈ baseline ±15% هنا — أقل إشكالًا من bucket +27%).
  2. إعادة تقييم حكم T5-B على Bucket تُترك للمالك: فوز الـquiet day حقيقي (77×) وفائدة العاصفة غير متكوّرة؛ مع C1 تصبح فائدة bucket العاصفية ≈ 0.3% فقط.
  3. ممنوع أدوات القياس الثقيلة على مقاييس العاصفة (الـheavy probe): أثبتت فاتورة +28%/+125% — البديل المعتمد في هذا العدّاء (التقاط خارج التكة بخطي).
  4. أتمتة العاصفة كـregression: `test_t5c_storm_lab.gd --phase=e0 --probe=off` مع بوابة SEM/SEQ/counters = مرجع دائم.

## 8) خريطة الأدلة (raw logs — كلها هذه الحملة)

| الدليل | المسار |
|---|---|
| E0 current OFF (OSB) | `.ai/evidence/tests/test_t5c_e0_current_off.log` |
| E0 bucket OFF (BSB) | `.ai/evidence/tests/test_t5c_e0_bucket_off.log` |
| E0 current ON | `.ai/evidence/tests/test_t5c_e0_current_on.log` |
| E0 bucket ON | `.ai/evidence/tests/test_t5c_e0_bucket_on.log` |
| E1 تفكيك current | `.ai/evidence/tests/test_t5c_decompose_current.log` |
| E1 تفكيك bucket | `.ai/evidence/tests/test_t5c_decompose_bucket.log` |
| C1 current / bucket | `.ai/evidence/tests/test_t5c_c1_current.log` / `test_t5c_c1_bucket.log` |
| C2 ظل K=∞ | `.ai/evidence/tests/test_t5c_c2_validate_current.log` |
| C2 مقطّع K=5000 / K=1000 | `.ai/evidence/tests/test_t5c_c2_sliced5000_current.log` / `test_t5c_c2_sliced1000_current.log` |
| E4 baseline + m29/m31/spread | `.ai/evidence/tests/test_t5c_e4_baseline35_current.log` / `…_m29.log` / `…_m31.log` / `…_spread.log` |
| العدّاء | `scripts/test_t5c_storm_lab.gd` |
| التجريبية | `scripts/experimental/{event_queue_probe, scheduler_probe_lite, batched_event_queue, sliced_runner}.gd` |

## 9) سجل المراجعة

- rev.1 (2026-09-05): التنفيذ الكامل وفق خطة rev.2 المعتمدة (E0 أساس رسمي منزوع القياس → E1 بوابة حاكمة → C1/C3 بقرار جدول مجمد → C2 بفصل duration المغايرين → E4 ضابط سلبي). صفر تعديل نواة (git diff على الملفات التسعة فارغ). جميع البوابات bitwise.
