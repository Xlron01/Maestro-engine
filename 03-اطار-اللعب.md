# Gameplay Framework — محتوى اللعبة فوق المحرك

كل ما في هذه الوثيقة هو **محتوى (Content)**، وليس جزءًا من المحرك — مبني بالكامل كملفات بيانات تُقرأ عبر آليات Architecture (Variable Bus، Tiered Scheduler، Serialization...). لعبة أخرى مختلفة الموضوع يمكنها استبدال كل هذا دون المساس بالمحرك.

---

## 1) نظام الأهمية الجغرافية/السياسية (محتوى الـ Relevance Graph)

المحرك يوفر بنية الجراف فقط؛ هنا كيف تُحسب الأوزان فعليًا لهذه اللعبة تحديدًا:

**رُفض** التقسيم الجغرافي الحرفي (خطوط طول/عرض) — فشل مع دول جزرية، مستعمرات، تحالفات عابرة للمحيطات.

**3 طبقات متكاملة:**

1. **Power Rank** (تحديث ربع سنوي/سنوي) → الأهمية الهيكلية (superpower/great/major/minor/none)، مبني على GDP/صناعة/جيش — مستوحى من نظام MD الموجود.

2. **Relevance boost** (تحديث شهري/أسبوعي وقت الأزمات) → يعدّل فوق Power Rank. مثال: بلجيكا 1940 ليست Great Power لكنها بؤرة حرجة كممر غزو. القيمة الاستراتيجية تُحسب مرة واحدة عبر **Betweenness Centrality** (مدى وقوعها على أقصر طريق بين نقاط مهمة) وتُخزَّن كقيمة ثابتة، لا تُعاد حسابًا حيًا.

3. **Render Layer** (منفصل تمامًا عن الحساب) → يحدد فقط ما يراه اللاعب بصريًا. البؤرة الحسابية **دائمًا عالمية**، لا تتبع موقع اللاعب — وإلا حروب كبرى بعيدة عن رؤيته ستُحسب برخص غير واقعي.

**قابلية التأثر بالخارج**:
```
susceptibility_to_external_influence = base_factor / (power_rank + stability)
```
كل ما الدولة أقوى كل ما تأثير الخارج عليها أقل، والعكس.

---

## 2) معادلة أهداف الدولة (Goal Weights)

بديل "الذكاء الحقيقي" — معادلة وزن بسيطة تُحسب من بيانات موجودة أصلًا:

```
goal_weight[survival]      = f(military_threat_nearby, border_security)
goal_weight[power_max]     = f(current_power_rank, growth_rate)
goal_weight[security]      = f(num_hostile_neighbors, alliance_coverage)
goal_weight[prosperity]    = f(economic_stability, trade_dependency)
goal_weight[prestige]      = f(ideology, government_type, historical_flag)
goal_weight[ideological]   = f(ruling_party_ideology, expansionist_trait)
             -= corruption_level * foreign_influence_penetration * susceptibility_to_external_influence

primary_goal = argmax(goal_weights)
```

**اختيار الوسيلة** (عسكري/استخباراتي/دبلوماسي) — Expected Utility بسيط:
```
utility[option] = (probability_of_success * value_if_success)
                 - (cost_over_time * duration_penalty)
                 - (risk_of_failure * severity_if_failed)
                 - (expected_retaliation)   ← فقط من الدول المرتبطة عبر Relevance Graph
```

**التسلسل الهرمي**: Goal → Strategy → Means، يعكس نفس منطق هرمية القيادة العسكرية (قسم 4).

**نظام الحكم كنتيجة تراكمية لا مدخل**:
```
Conditions → Goal Weights → Chosen Actions → (Stability, Legitimacy, Ideological Purity) → Regime Type يتغير بمرور الوقت
```
دولة بهدف Survival قوي + استقرار منخفض → قمع داخلي → تآكل شرعية تدريجي → تحول النظام. نفس حلقة معادلة الانقلاب (قسم 3) من زاوية أخرى.

---

## 3) معادلة الانقلاب (بديل العشوائية الحالية في HoI4)

```
coup_risk_score =
    + (war_exhaustion * 0.3)
    + (ideology_support[coup_ideology] * 0.4)
    - (ideology_support[current_ideology] * 0.2)
    + (party_popularity[coup_faction] * 0.25)
    - (stability * 0.5)
    + (foreign_support_for_coup * 0.15)
    + (working_age_population_urban * ideology_mismatch) * 0.2
    - (literacy * 0.1)
    + (war_generation_loss * 0.15)
```
كل عنصر مبني على متغيرات مخزَّنة أصلًا في الـ Variable Bus — بلا حسابات جديدة. حدث شهري لعدد محدود من الدول غير المستقرة فقط (Tier_Fast).

**ملاحظة**: كل الأوزان (0.3, 0.4, 0.5...) أرقام أولية تحتاج توازنًا فعليًا بالتجربة — تُخزَّن في `data/rules/politics.json` قابلة للتعديل دون إعادة بناء.

---

## 4) نظام Demographic Cohorts

بديل الـ manpower كرقم واحد. لكل state (لا لكل دولة، لدقة جغرافية) 6-7 أرقام فقط:
```
youth (0-17), working_age (18-45), mature (46-64), elderly (65+),
urban_ratio, literacy
```
تحديث كل ربع/نص سنة، تتحول لـ modifiers محسوبة (`effective_manpower`, `effective_factory_output`) يقرأ منها باقي الأنظمة.

---

## 5) الطبقة العسكرية: هرمية القيادة

سبب اللاج في HoI4 (جيش كبير/رؤية واسعة = سخونة): كل division كائن مستقل يحسب pathfinding/combat/vision بنفسه — قرار معماري قابل للتجنب (دليل: Songs of Syx يدير عشرات آلاف الوحدات بسلاسة بمعمارية مختلفة).

**الحل: هرمية قيادة حقيقية بدل وحدات مستقلة**:
```
Theater/Front  → قرار استراتيجي (نادر التغيير)
Army Group     → قرار عملياتي (كل أسبوعين-شهر)
Corps          → مناورة تكتيكية (كل كام يوم)
Division       → تنفيذ محلي محدود بأوامر الـ Corps (يومي، ضمن نطاق ضيق)
```
كل ما زاد التنظيم قلّ عدد نقاط القرار المستقلة فعليًا؛ كل ما قلّ التنظيم (ميليشيات/دول ضعيفة) كان العدد نفسه صغيرًا طبيعيًا. المعادلة: (درجة الاستقلالية × عدد الوحدات) تقريبًا ثابتة أو تقلّ مع زيادة القوة — عكس افتراض HoI4 الحالي.

**تطبيقات**:
- رؤية/استخبارات هرمية: تقارير مجمّعة على مستوى Army Group، لا رصد فردي لكل وحدة معادية.
- الاستثناء الوحيد: نقطة التماس (خط جبهة نشط) — دقة أعلى محليًا فقط، بنفس مبدأ Tiered Scheduler.
- جبهات متعددة (حرب عالمية): كل جبهة = Activity Cluster مستقل، مربوط بالجبهات الأخرى فقط عبر موارد مشتركة (مثل manpower قومي) لا تفاعل تفصيلي مباشر.

---

## 6) الشخصيات/القادة (Characters/Leaders)

نظام مقابل لفكرة "الجنرالات القابلين للترقية" من التصور الأصلي للمود:
- `data/characters/<country>.json` — بيانات كل شخصية (اسم، صورة، سمات/traits كأرقام).
- الـ traits تُسجَّل كـ modifiers في الـ Variable Bus (مثل: `aggressive_commander: +10% attack`) — تستفيد من نفس البنية التحتية دون نظام جديد.
- الصور = Assets عادية بنفس آلية تحميل الموارد.

---

## 7) الشفافية (Explainability)

طبقة عرض فقط — tooltip يسحب قيم متغيرات مخزَّنة أصلًا ويعرضها نصًا (مثال: "دعم الحزب: +15 | الاستقرار: −20 | دعم أجنبي: +10"). ليست أساسًا لاتخاذ القرار الفعلي — أرخص وأوضح للاعب.

---

## 8) بنية الخريطة (محتوى)

ثلاث مستويات تجميع صريحة تُبنى في بيانات الخريطة نفسها (وليس محسوبة وقت التشغيل):
```
province → strategic_region → supply_area
```
مطابقة لمستويات الهرمية العسكرية (قسم 5)، تُسهّل الـ Hierarchical Pathfinding الموفَّر من المحرك.

---

## 9) عناصر مؤجَّلة لمراحل لاحقة (خارج الـ MVP)

- **Equipment Designer** (تصميم دبابة/طائرة تفصيليًا) — نظام Gameplay اختياري ضخم؛ أول لعبة تستخدم معدات ثابتة محددة مسبقًا لاختبار الـ Core أولًا.
- **الأنظمة التسعة الأصلية الكاملة** (شركات، سوق سوداء، إعلام، سايبر، هجرة، عقوبات...) — تُبنى تباعًا فوق نفس البنية بعد إثبات نجاح Prototype أول نظام (الانقلابات مقترحة كبداية).
- **موسيقى/صوت**: state → playlist mapping بسيط فوق Godot AudioStreamPlayer، أولوية منخفضة.
- **تعدد لغات فعلي**: البنية (مفاتيح الترجمة) تُبنى من اليوم الأول، لكن الترجمة الفعلية تقتصر على لغة أو لغتين بالبداية.

---

## أول Prototype مقترح

Relevance Graph + Tiered Scheduler (من Architecture) + معادلة الانقلاب (قسم 3 هنا) كنموذج مصغّر متكامل — أصغر وحدة تثبت أن المبدأ العام (بيانات مخزَّنة + معادلات وزن + جدولة حسب النشاط) يعمل فعليًا قبل التوسع لباقي الأنظمة.
