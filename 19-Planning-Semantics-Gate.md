# 19 — Planning Semantics Gate 1

> **وثيقة حسم تحليلية — صفر كود، صفر Planner، صفر Simulation integration، صفر تشغيل.**
> تتبع: Gate 1 (doc 15 §0 — القيمة للـOutcome لا الفعل) · Gate 2 (doc 11 — لا Trajectory primitive + شروط R1–R3) · docs 16–18 (الحدود والدلالة والمواصفة المجربة bitwise).
>
> **السؤال الأنطولوجي المجمد (نص المالك حرفيًا):**
>
> **ما أقل تمثيل تنبؤي مطلوب لكي يستطيع Decision System مقارنة Actions بناءً على نتائج مستقبلية، دون إدخال Primitive جديد للمسار؟**

> [!IMPORTANT]
> **الفجوة المؤسِّسة:** السلسلة اكتملت حتى «لو عندي معلومات عن النتائج المحتملة أقيّمها» — لكن لا شيء يجيب «لو نفذت X ماذا سيحدث للعالم؟». هذه البوابة تحسم دلالة الجواب، ولا تبني الجهاز.

---

## 0) المبدأ الحاكم — العقد الثلاثي الفاصل

```text
                    ┌── Prediction ──→ Outcomes
World ── Action ─┤
                    └── ...
                            ↓
                       Evaluation      ← doc 18 المجمدة
                            ↓
                         Decision     ← يختار
                            ↓
                     Execute Action    ← الطريق الوحيد الذي يكتب WorldState
```

**Planner يتنبأ · Evaluation يقيّم · Decision يختار — ولا واحد من الثلاثة يكتب في WorldState الحقيقي أثناء التفكير.**

> **«التفكير لا يلوث المحاكاة»** — امتداد مباشر لخاصية P6 المثبتة في D1، مرفوعة من خاصية طرفية إلى عقد معماري أمامي.

## 0-b) الضوابط المجمدة (بتوجيهات المالك اليوم)

1. **منع البناء لا الذكر**: تحليل «clone-and-replay للقواعد الموجودة» كمرشح دلالي داخل حدود البوابة مشروع؛ الممنوع بناؤه أو دمجه فقط.
2. **Open-للعجز-فقط**: خانة السؤال الفرعي الواحد لا تُحرق وقائيًا — تُفتح فقط عند عجز مثبت.
3. Temporal carve-out موروث؛ deg/degree غير منطبقة (لا تشغيل)؛ الإغلاق يتبعه **توقف كامل**.

---

## 1) القواعد الإلزامية

1. **سلم المرشحين المجمد (§2) هو مسار الحسم بترتيب الحذف** — لا مرشح خارج السلم ولا قفز بلا deletion test موثق.
2. **مفردات واصف التنبأ مغلقة حصرًا** (doc 17 §2.2: Model v1 + content schema + R1–R3) — مضاد التهريب يمتد من التقييم إلى التنبؤ؛ أي حساب جديد داخل «الحالة المتنبأة» = evaluator/predictor مقنّع.
3. **النسخة الافتراضية موسومة صراحة** (`hypothetical=true` إجرائيًا) ولا تُخلط بالعالم الحقيقي في أي مسار قراءة/كتابة — الوسم عملية انضباط لا مفهوم دلالي.
4. **وراثة نصية ملزمة**: Gate 1 (التفضيل للنتائج المستقبلية لا للأفعال) · Gate 2 (**المسار ليس primitive قيمة**؛ كل معلومة عملية تتحلل إلى حالات مؤرخة/عدادات/التزامات — شروط R1–R3 باقية نصًا).
5. **Rejected يتطلب إثباتًا**: أي درجة من السلم لا تُحذف إلا بإثبات فشلها على counterexample حقيقي موثق — والحذف يشمل تسجيل «أين بالضبط فشل».
6. **L1 يمتد أماميًا**: التنبؤ **لا يحاكي نوايا أو ردود الأفعال الآخرين** — ردود الأفعال قرارات مستقبلية لتلك الكيانات، كلٌّ بخطته الخاصة؛ التفاعل طوارئ وليس محاكاة داخل خطتي.
7. **P5 خاصة — الحتمية أولًا**: المحرك مجمد وحتمي؛ التوزيع الاحتمالي ليس افتراضًا تلقائيًا ويحتاج إثبات عجز الحتمية عنه قبل أي ذكر.
8. كل نتيجة مصنفة إلزاميًا: Confirmed / Rejected / Open.
9. Definition / Runtime State / Historical Change مفصولة دائمًا.
10. **صفر كود وصفر Planner وصفر integration** — الإغلاق CONFIRMED يفتح الباب لـPlanning Specification v0.1 **بأمر منفصل** لا قبل.
11. الأسئلة الأوسع (بحث/أفق/تفريع/تكلفة/pruning) تعود لطبقة Spec — لا تُحسم هنا.

---

## 2) سلم المرشحين المجمد (تعريف حرفي) + التصنيفات المتوقعة

### 2.1 الدرجات

**(أ) Declared-Delta Descriptors** — الفعل يحمل في المحتوى وصف نتيجته معلنًا (نمط Test E): `descriptor(a) = content.declared_outcomes` — بلا أي حساب حالة.

**(ب) Clone-and-Replay of Existing Rules** — التعريف التنفيذي:

```text
predict(action a, world w_real):
  h := deep_copy(w_real)              # النسخة الافتراضية
  h.hypothetical = true               # وسم إجرائي (قاعدة 3) — لا يدخل أي واصف
  apply_existing_dispatch_handlers(a, h)   # نفس القواعد الحتمية الموجودة — صفر قواعد جديدة
  return descriptor_from(h)           # قراءة بمفردات doc17 المغلقة حصرًا
```

**(ج) Per-Action Forward-Models (STRIPS-style)** — precond/add/delete معلنة لكل نوع فعل: تمثيل جديد كليًا؛ لا يدخل السلم إلا بإثبات فشل (ب) عليه عبر Deletion Test موثق.

### 2.2 التصنيفات المتوقعة المجمدة (قبل التحليل — قابلة للكسر)

| CE | التوقع المجمد | شرط الكسر |
|---|---|---|
| P1 فعل يغير fact | ✅ (أ) تكفي **محليًا لهذا الـCE** — دلتا معلنة كاملة ممكنة يدويًا لحالة أحادية | لو حتى الحالة الأحادية احتاجت حقائق مشتقة غير قابلة للإعلان |
| P2 Preconditions بعد A | (أ) تفشل بنيويًا (اكتمال الحقائق التي ستقرأها الشروط لاحقًا غير مضمون) ⇒ ✅ عبر (ب) + إعادة تشغيل مطابقة الشروط الموجودة على الحالة المتنبأة | ظهور بديل أبسط من (ب) يضمن الاكتمال |
| P3 سلسلة A→B→C | ✅ اختزال إلى **تكرار خطوة-(ب) + بوابة preconditions بين الخطوات** — لا كائن Plan (وراثة Gate 2 حرفًا) | ظهور حاجة لتخزين «الخطة» ذاتها في الواصفات = تهريب Trajectory |
| P4 آثار جانبية | **قاتل (أ)**: الاكتمال مستحيل يدويًا لأن الآثار تطفو من تفاعل المعالجات ⇒ ✅ عبر (ب) اكتمالًا بالبناء | ظهور (أ)-معززة تغلق الفجوة دون ازدواج مصدر الحقيقة |
| P5 عدم اليقين | ✅ **Confirmed-deterministic** — أثر فعلي الميكانيكي نتيجة واحدة بحتمية المحرك؛ ردود الآخرين خارج نطاقي (قاعدة 6) | ظهور محتوى عشوائي معلن في المحرك الحالي (لا يوجد) |

---

## 3) التحليل التطبيقي

منهجية كل حالة: **تمثيل** على السلم → **اختبار الاختزال** → **فحص المقنّع** → **حكم واقعي** → **تصنيف + مصير الدرجات**.

---

### CE-P1 — Action يغير Fact واحد

**التمثيل:** فعل «حظر تصدير EUV_flow» يعلن دلتا `{possession[EUV_gate]: −δ}` على Maker. الواصف المتنبأ يُقرأ مباشرة.

**اختبار الاختزال:** القيمة كلها في تغير حقfact واحد معلن — (أ) يغطيه بلا آلة.

**فحص المقنّع:** لا حساب خفي؛ الدلتا نفسها محتوى. ✔️

**حكم واقعي:** قرار أمريكي منفرد بسحب ترخيص شركة واحدة — الأثر الأولي حقfact مباشر قابل للإعلان ✔️.

**الحكم:** ✅ **Confirmed عبر (أ) محليًا** — طابق التوقع. *(ملاحظة مصير: كفاية (أ) هنا محلّية لا عامة — المصير النهائي للدرجة يُحسم في P4.)*

---

### CE-P2 — Precondition يعتمد على فعل سابق

**التمثيل:** B «تحصين الممر» لا يتاح إلا بعد A «تثبيت حليف على البوابة». توافر B = مطابقة شروط `{field,op,value}` (نمط Gate 15 §3.2) على **حالة ما بعد A**.

**اختبار الاختزال — أين تفشل (أ):** توافر B يقرأ حقائق محددة لا يعرف مؤلف المحتوى مسبقًا أي منها ستسأل عنه شروطُ الأفعال اللاحقة. ضمان الاكتمال تحت (أ) يعني إعلان «كل الحقائق التي قد تُقرأ مستقبلًا» = إعادة بناء الحالة كاملة يدويًا = (ب) بأدوات هشة. **إذن (أ) يفشل بنيويًا هنا — Rejected لهذا الـCE.**

**(ب) يغطيه:** `h=predict(A)` ثم مطابقة شروط B على h بنفس الآلية الموجودة — صفر مفردات جديدة.

**Deletion Test لـ(ج) مبكرًا:** قوائم add/delete يدوية = **مصدر حقيقة ثانٍ** يكرر ما تحسبه المعالجات أصلًا — انحراف مضمون مع كل تطوير handler، وقدرة صفرية فوق (ب). ❌ يُرفض دون بناء.

**حكم واقعي:** ممر البحر الأحمر: توافر «العبور الآمن» مشروط بحقائق حضور التحالف المثبَّت، لا بإعلانات منفصلة ✔️.

**الحكم:** ✅ **Confirmed عبر (ب)** — (أ) Rejected هنا. *(طابق التوقع.)*

---

### CE-P3 — سلسلة A→B→C

**التمثيل:**

```text
S1 = predict(A, W_real)        # إن فشلت شروط A ⇒ السلسلة غير صالحة من الخطوة الأولى
S2 = predict(B, S1)            # شروط B تُطابق على S1 (آلية CE-P2)
S3 = predict(C, S2)
outcome_descriptor = descriptor_from(S3)
```

**اختبار الاختزال:** «الخطة» موجودة **كترتيب استدعاءات في التفكير فقط** — لا كائن Plan يُخزن ولا يدخل الواصف. القيمة كلها في واصف الحالة النهائية + بوابطات التوافر بين الخطوات (P2). أي تخزين للتسلسل ذاته = تهريب Trajectory الذي حسمه Gate 2 نصًا.

**فحص المقنّع:** هل نحتاج حفظ S1/S2 بعد اجتياز بوابتَي التوافر؟ لا — يكفي أنهما أثبتا الصلاحية؛ البقايا الوحيدة المشروعة ما يستقر في S3 من سجلات R1/R2/R3. ✔️

**حكم واقعي:** السيادة على الرقائق: بناء fab → نقل المعرفة → إنتاج — لا فعل منفرد يحققها، ولا «خطة» مخزنة تتطلبها الدلالة، بل سلسلة تطبيقات تُحكم ببوابات التوافر ✔️.

**الحكم:** ✅ **Confirmed — iterated-(ب) + gating، بلا Plan primitive** — طابق التوقع. (آليات البحث/الأفق/التفريع = طبقة Spec، قاعدة 11.)

---

### CE-P4 — الآثار الجانبية (المحكمة الحقيقية)

**التمثيل:** فعل «مقاطعة ناجحة» يحقق الهدف (انخفاض rel_supply عن المورد) لكنه يستنزف عداد احتياطيات (R1)، يرفع exposure عبر إعادة توزيع حصص (Model v1)، ويولد حدث انتشار يعدّل استقرار طرف ثالث.

**اختبار الاختزال — إعدام (أ) رسميًا:** تعداد هذه الآثار يدويًا = إعادة كتابة منطق المعالجات خارجها؛ **ازدواج مصدر الحقيقة** يضمن الانحراف عند أول تعديل handler. الفشل هنا ليس حظًا سيئًا بل بنيوي: الآثار **تطفو من التفاعل** لا من الإعلان. ⇒ **(أ) Rejected كدلالة عامة** (Deletion Test منجز على حالة واقعية).

**(ب) يكتمل بالبناء:** تشغيل نفس المعالجات على النسخة يحسب ما سيحسبه المحرك نفسه — بما فيه تحديثات Model v1 وسجلات R1–R3 بشروط doc 11.

**فحص المقنّع:** هل (ب) يقدّم قدرة جديدة تستحق primitive؟ لا — صفر مفردات جديدة؛ هو **تنفيذ معروف على نسخة موسومة**. ✔️

**حكم واقعي:** مقاطعات تاريخية نجحت في هدفها وخلفت أزمة إمداد مضادة واستنزاف مخزون — آثار شبكية لم يعلنها أحد مسبقًا ✔️.

**الحكم:** ✅ **Confirmed عبر (ب) — و(أ) تخرج من السلم Rejected** — طابق التوقع.

---

### CE-P5 — عدم اليقين

**التمثيل المقترح المخيف:** `Action → distribution(outcomes)` بدل حالة واحدة.

**اختبار الحتمية أولًا:** المحرك المجمد حتمي (لا عشوائية في المحتوى)، وأثر الفعل الميكانيكي المباشر تحت معالجات موجودة **نتيجة واحدة**. مصدر «عدم اليقين» الوحيد الممكن = ردود الكيانات الأخرى — وهي **قرارات مستقبلية لها** (قاعدة 6/L1): كلٌّ يتنبأ ويخطط بخطه؛ التفاعل يظهر عند التنفيذ لا داخل خطتي. إذن التوزيع لا يملك مصدرًا دلاليًا في النظام الحالي.

**فحص المقنّع العكسي:** هل قمع التوزيع يخفي شيء؟ لو أدخل المحتوى مستقبلًا عشوائية معلنة (نرد/احتمالات)، فذلك امتداد محتوى+Gate خاص — لا ثغرة هنا لأننا لا نؤجل حلًا موجودًا بل نؤكد غياب الحاجة الراهنة. ✔️

**حكم واقعي:** حصار قديم: نتيجة كسر الأسوار ميكانيكيًا محسومة بقواعد الحصار؛ «ماذا سيفعل المحاصر؟» سؤال خطته هو لا خطتي ✔️.

**الحكم:** ✅ **Confirmed-deterministic — بلا حرق خانة Open** (وفق قرار المالك: Open للعجز فقط، والعجز لم يظهر). *(مسجل: أي عشوائية محتوى مستقبلية ⇒ Gate مستقل.)*

---

## 4) الحصيلة ومصير السلم

| الدرجة | المصير | الدليل |
|---|---|---|
| (أ) Declared-deltas | ❌ **Rejected كدلالة عامة** | فشل بنيوي في P2 (الاكتمال) وP4 (الآثار الشبكية) — ناجٍ محتمل لاحقًا كـannotation تحسيني في طبقة Spec لا كدلالة |
| (ب) Clone-and-Replay | ✅ **Confirmed — الحد الأدنى الكافي** | P2/P3/P4 كاملة عبره بصفر مفردات جديدة؛ يعيد استخدام الحتمية والdispatch وشروط المطابقة الموجودة |
| (ج) Forward-models | ❌ Rejected دون بناء | Deletion Test: مصدر حقيقة ثانٍ مكرر، قدرة صفرية فوق (ب) |

**نتيجة البوابة:** أقل تمثيل تنبؤي = **تطبيق حتمي للقواعد الموجودة على نسخة افتراضية موسومة، وقراءة الواصف بالمفردات المغلقة، مع بوابات توافر بين الخطوات** — صفر primitive جديد للمسار أو الخطة أو التوزيع. امتثال Gate 1/Gate 2 نصًا.

**العمارة المكتملة أول مرة:** WorldState → Relevance → Goals/Preferences → Candidate Actions → **Prediction (ب)** → Candidate Outcomes → Evaluation → Decision → Execute → WorldState′.

### 4-b) إفصاح صريح — (ب) لم يخضع لاختبار عدائي مباشر

**(ب) clone-and-replay نجا من السلم دون أن يُهاجَم بالشراسة التي هوجم بها (أ) و(ج).** تصنيف مزدوج بأمانة:

1. **القرار واعٍ بنطاق البوابة:** سؤالها «أقل تمثيل **كافٍ**» لا «الأمثل تحت كل الظروف». الأسئلة العدائية الميكانيكية — search/branching/horizon/مقارنة تكاليف تراكمية عبر مسارات متعددة — تسأل «كم حالة افتراضية نستكشف وكيف؟» وهي آلية استكشاف في طبقة Spec (قاعدة 11)، لا تسأل «ما هي الحالة المتنبأة ذاتها؟» التي تحسمها Semantics. على هذا المحور: غياب الهجوم قرار منهجي مقصود.
2. **الاستثناء المسمى (لا يُدفن):** partial-observability هو الاتجاه العدائي الوحيد الذي **يمس التمثيل نفسه** (واصفُ مَن يرى ماذا؟)، ويقاطع فجوة الرؤية غير المتماثلة الموثقة في doc 13 ⇒ مسجل صراحة كسؤال مؤجل ضمن 6.4، يتطلب Gate خاصًا عند الاقتراب من التنفيذ.
3. **قيد القراءة الملزم:** نجاح (ب) في CEs الخمسة **برهان كفاية لا ملاءمة شاملة** — ممنوع توظيفه لاحقًا دفاعًا عن (ب) ضد partial-observability أو أي فئة آلية مستقبلية.

## 5) حالة البوابة

⏸️ **PROVISIONAL — بانتظار اعتماد المراجع بعد إيصال إفصاحين (4-b ومحضر validator الخام)** — الحالة بيد المراجع لا بيد الوثيقة

### سجل المراجعة

| التاريخ | الإجراء | السبب/التوكيد |
|---|---|---|
| 2026-08-26 | فتح البوابة باختيار المالك: **Planning قبل D3** — لأن السؤال انتقل من «كيف تختار الدولة؟» إلى «كيف تعرف ماذا سيحدث لو اختارت؟» | D3 المباشرة كانت ستبني تقييمًا على deltas معلنة ثم تُعاد كتابته عند ظهور الحاجة للحالة المتنبأة — نفس الفخ الذي جهزت له Gates 1/2 |
| 2026-08-26 | تجميد سلم المرشحين (أ)/(ب)/(ج) في §2 قبل التحليل | قرار المالك: تجميد السلم يجعل التوقعات أشد ويمنع انزلاقًا لاحقًا |
| 2026-08-26 | تثبيت حدود «لا integration»: منع البناء لا الذكر | clone-and-replay مرشح دلالي مشروع داخل البوابة؛ بناؤه/دمجه فقط هو المحظور |
| 2026-08-26 | تثبيت سياسة خانة Open: للعجز المثبت فقط — لم تُحرق في P5 | الحتمية كفت الحالة؛ أي عشوائية محتوى مستقبلًا ⇒ Gate مستقل |
| 2026-08-26 | تحليل CEs الخمسة ⇒ **5/5 Confirmed مطابق للتوقعات، صفر كسر** ⇒ إغلاق Confirmed + توقف كامل | (أ) أُعدمت بP2/P4، (ج) رُفض بdeletion test، (ب) نجت كالحد الأدنى الكافي |
| 2026-08-26 | **rev.2 بمراجعة المالك قبل الختم**: (أ) إفصاح 4-b — (ب) لم يُختبر عدائيًا مباشرة؛ قرار نطاق واعٍ للأسئلة الآلية + تسمية partial-observability استثناءً يمس التمثيل (يقاطع فجوة doc 13) مؤجلًا لGate خاص + قيد قراءة «كفاية لا ملاءمة شاملة»؛ (ب) الحالة أعيدت PROVISIONAL واقعيًا؛ (ج) محضر validator الخام منقول حرفيًا قبل سطر Evidence trail | نفس منطق «لا استنتاج ضمني» المنطبق على doc18 — الفجوة المريحة تُصرّح لا تُسكَت |
| 2026-08-26 | **rev.3 — تصحيح إيبيستيمي لأساس قاعدة 6 (بطلب المالك — سطر جديد لا يمس §4-b المغلقة)**: أساس قرار منع محاكاة النوايا داخل الخطة يُسجل الآن حرفيًا: **«قرار تصميمي متوافق مع L1 المُقفلة، تدعمه مؤشرات بحثية عامة (opponent modeling) غير مُتحقق منها على حالتنا — ليس حجة مثبتة علميًا»**. صحب المالك صياغة أقوى سابقة («الأدلية تثبت»/«تعقيد أسي»)؛ القرار نفسه قائم بلا تغيير. وضع العلاقة ب§4-b: هذا **تصحيح على مستوى تسجيل الأساس في السلسلة كلها لا على نص مغلق**؛ تأجيل partial-observability المذكور في §4-b باقٍ كما هو بلا أي تعديل | مبدأ: لا تعديل مباشر على نص مغلوق — سطر جديد يوثق التصحيح |

## 6) Open Questions والالتزامات المستقبلية

1. **Open الوحيد المتبقي عبر السلسلة:** أنطولوجيا مصدر الأفعال (موروث doc 16).
2. **البوابة التالية المرخصة منطقيًا (بأمر منفصل):** Planning Specification v0.1 — تجيب «كيف نبني هذا التخطيط؟»: state representation · transition mechanism · search · horizon · branching · cost · pruning.
3. **التزام مستقبلي مسجل — Planning Generalization / Behavioral Validation Gate (بأمر منفصل بعد بناء Planner مطابق للـSpec):**

> **السؤال القابل للقياس:** هل يستطيع الـPlanner إنتاج قرارات/خطط صحيحة في مجموعة سيناريوهات مستقلة **تختلف في بنية المشكلة لا في القيم فقط؟**
>
> **أطياف السيناريوهات الثمانية المجمدة للحين:** هدف مباشر بفعل واحد · سلسلة أفعال · فعل سيئ-ظاهريًا يفتح نتيجة أفضل · فعل ممتاز محليًا كارثي متأخر · قيود تمنع أقصر طريق · خطط صحيحة متعددة بتكاليف مختلفة · تغيير العالم بثبات Goal · تغيير Goal بثبات العالم.
>
> **قاعدة الـfixture الحاكمة:** الاختبار يحدد {العالم + الهدف + الأفعال + القيود + معيار النجاح} **ولا يتضمن الحل أبدًا** — كتابة «المفروض A→B→C» داخل الـfixture تحوّل الفحص إلى اختبار implementation لا Planning.
>
> **سلّم الطبقات وادعاءاتها المنفصلة (كل طبقة ادعاؤها ودليل يناسبها):**
> `Gate 1 → Semantics` ← ثم `Spec → Architecture/Mechanism` ← ثم `Implementation Tests → Correctness` ← ثم `Generalization Gate → Behavioral adequacy`
> فشل الـPlanner لا يفنّد Semantics صحيحة، ونجاحه لا يكتمل بها — الادعاءات غير قابلة للتبادل.

*(حدود التسجيل: التزام واحد في §6 — لا counterexample سادس ولا نقاش Generalization داخل متن هذه البوابة، ولا يؤثر على أي معيار قبول هنا.)*

4. موروث باقٍ: ربط الواصفات بمحاكاة حقيقية بدل fixture-deltas — مسألة تتقاطع مع (ب) أعلاه وتتطلب Gate خاصًا بها عند الحاجة.

---

**محضر validator الخام (منقول حرفيًا من الإخراج الفعلي المسترٌ إلى ملف — 2026-08-26؛ بوابة بلا تشغيل محرك: هذا إخراج أداة الذاكرة لا لوج محاكاة):**

```text
====================================================
  Maestro Memory Validator & Linter
====================================================
Checking project structure...
Checking task files...
Checking link integrity...

----------------------------------------------------
Validation Finished: 0 Errors, 1 Warnings
----------------------------------------------------

Warnings:
  [WARN] Broken file link in .\00-خطة-الطريق.md: c:/tmp/maestro%20engine/acceptance_report.md (Expected file: acceptance_report.md)

[SUCCESS] Memory integrity validation passed successfully!
```

*(ملاحظة صدق: التحذير الوحيد موروث خارج نطاق هذه البوابة؛ سطر اسم الملف العربي داخله قد يظهر محرفًا حسب ترميز الكونسول — منقول كما أُلتقط.)*

---
---
**Evidence trail:** docs 11/15/16/17/18 + هذا الملف. صفر أدلة تشغيلية — بوابة تحليلية بحتة.
