# Architecture — التفاصيل المعمارية

هذه الوثيقة تفصّل *كيف* تُطبَّق مبادئ Engine Principles تقنيًا. قابلة للتعديل مع التعلم والتجربة، بخلاف الوثيقة الأولى.

## البنية العامة (Godot كأساس)

```
Godot Engine (rendering, input, audio, export)
        ↓
"محركنا" الفعلي = طبقة وسطى فوق Godot:
   - نظام خريطة provinces قابل لإعادة الاستخدام
   - نظام Scripting/Data بسيط (data-driven)
   - Simulation Core (كل الأنظمة أدناه)
   - إطار UI أساسي
   - Serialization Layer عامة
        ↓
لعبة 1 → لعبة 2 → لعبة 3 (محتوى مختلف، نفس المحرك)
```

**Godot** اختيار الأساس: مفتوح المصدر، مجاني، أخف من Unreal (لا حاجة لـ 3D ثقيل)، مناسب لواجهات 2D-heavy (خرائط، قوائم). واجهة "تثبّتها وتصمم بها" ممكنة عبر بناء **Editor Addons/Plugins** داخل محرر Godot نفسه.

## فصل الملفات: Repo منفصل للمحرك

```
my-strategy-engine/     ← المحرك (Godot addon، repo منفصل)
my-first-gsg-game/      ← اللعبة (repo منفصل، تستورد المحرك كـ submodule)
    ├─ engine/           (submodule)
    ├─ assets/textures/  (.png فقط)
    ├─ data/
    │   ├─ map/
    │   ├─ countries/
    │   ├─ rules/         (كل المعادلات كأرقام/معاملات)
    │   ├─ events/
    │   └─ scenarios/
    ├─ ui/scenes/
    └─ localisation/
```

القاعدة العملية لكل ملف كود: *لو اتشال، هل لعبة تانية مختلفة موضوعيًا هتحتاجه؟* أيوه → المحرك. لأ → اللعبة.

## مطابقة أنواع ملفات HoI4 بمكافئات Godot

| نوع HoI4 | المكافئ في تصميمنا |
|---|---|
| .png | .png مباشرة (Godot يدير الضغط تلقائيًا) |
| .dds | غير مطلوب (Godot يتولى الأمر) |
| .gfx (ربط اسم↔صورة) | غير مطلوب (Godot resource path تلقائي) |
| .gui | Godot Scenes (.tscn) |
| common/*.txt | JSON/Resources في `data/` |
| events/*.txt | GDScript في أول لعبة، ثم Scripting Runtime مخصص لاحقًا |
| history/*.txt | `data/scenarios/<name>/` — نفس آلية الـ Scenario Loader (جزء من Serialization Layer) |
| localisation/*.yml | Godot Translation (.po)، كل نص = مفتاح لا نص مباشر |

## الأنظمة الأساسية للمحرك (Core Systems)

### Central Variable/State Bus
أي قيمة غالية الحساب تُخزَّن مركزيًا. أي نظام آخر **يقرأ فقط**، لا يعيد الحساب.

### Tiered Scheduler (تحديث حسب النشاط)
```
Tier_Fast   (شهري)    → كيانات في أزمة/نشاط عالٍ
Tier_Medium (ربع سنوي) → نشاط متوسط
Tier_Slow   (سنوي)     → مستقر تمامًا (الأغلبية الساحقة غالب الوقت)
```
الانتقال بين الطبقات تلقائي حسب الحالة الفعلية (Activity-Driven Simulation).

**تفصيل تنفيذي — نموذج Sleep/Wake:** الصياغة العملية للـ Tiers هي أن كل كيان له حالة `Sleep` أو `Wake`. الكيان النائم لا يُعاد حسابه إطلاقًا حتى يستيقظ بفعل حدث. أمثلة معايير الاستيقاظ (Meaningful Thresholds، لا أي تغيير):
```
Supply 100 → 99          ⇒ لا يستحق الاستيقاظ
Supply Good → Degraded    ⇒ يستحق الاستيقاظ
```
معايير الاستيقاظ نفسها (القيم/العتبات) محتوى لعبة، لكن **آلية** Sleep/Wake والانتشار (Propagation) جزء من المحرك.

**قاعدة حاسمة لمنع الانهيار:** انتشار الاستيقاظ (مثال: تدمير خط سكة حديد → يوقظ شبكة الإمداد → يوقظ الوحدات المتأثرة فقط) يجب أن يتوقف عند **حدود التأثير الفعلي**، ولا يتسلسل بلا حدود عبر التسلسل الهرمي بالكامل. كسر هذه القاعدة (استيقاظ متسلسل غير محدود) يُعيد بالضبط مشكلة الأداء التي صُمم هذا النظام لتفاديها — نفس قاعدة O(الروابط ذات الصلة) تنطبق هنا أيضًا.

### Dirty Flagging
تحديث متغير يرفع "علم" أن الطبقة الأعلى قديمة؛ الحساب الفعلي يحدث عند دور الجدولة، لا فورًا. يمنع cascading recalculations.

### Relevance Graph (بنية عامة)
جراف عام (nodes + weighted edges) — المحرك يوفر البنية والاستعلام فقط؛ *كيف تُحسب الأوزان* (حدود، تحالفات، تجارة...) هو محتوى لعبة بالكامل (انظر Gameplay Framework).

### Hierarchical Pathfinding (HPA*) + رؤية هرمية
مسار خشن على مستوى تجميع أعلى + مسارات محلية رخيصة داخل نطاق ضيق، بدل مسار تفصيلي شامل الخريطة لكل كيان. يدعمه بنية خريطة بثلاث مستويات تجميع صريحة (province → strategic_region → supply_area)، مطابقة تمامًا لأي هرمية قيادة/تنظيم يحتاجها المحتوى.

### Serialization Layer
تُستخدم لـ: Save، تحميل Scenarios، Testing، وأي احتياج مستقبلي (Replay/Networking). تتضمن:
- **Schema Version** لكل ملف محفوظ.
- **Migration Manager**: عند تحميل نسخة قديمة، المحرك يسأل اللعبة "هل لديك قاعدة Migration لهذا الإصدار؟" وينفذها. **المحرك لا يستنتج قيم مفقودة بنفسه أبدًا** (لا Infer()) — فقط ينفّذ قواعد صريحة يوفرها ملف بيانات اللعبة. هذا يحافظ على الحتمية ويمنع تسريب منطق اللعبة للمحرك.

### Data Validation / Linting
عند تحميل أي ملف بيانات: **Error** يوقف التحميل فورًا لحقل مفقود/نوع خاطئ؛ **Warning** ينبّه ويكمل (مثال: "GDP أعلى من المتوقع لهذا النوع من الدول") دون توقف.

### Resource Loading Pipeline
معالجة موحدة لأي أصل يُحمَّل (ضغط/تخزين مؤقت) — يشمل الصور والصوت والخطوط بنفس الآلية العامة.

## تقنيات الأداء (مرجع سريع)

1. **LOD جغرافي وزمني** — كيانات بعيدة عن نقطة نشاط = تمثيل مجمّع رخيص.
2. **فصل Simulation Tick عن Render Tick** تمامًا.
3. **Spatial Partitioning** (grid/quadtree) — فحوصات القرب محدودة الجوار لا O(n²).
4. **Event-Driven بدل Polling** كل فريم.
5. **Aggregation للخلفية** — تمثيل إحصائي إلا عند دخول بؤرة نشاط.
6. **Multithreading حسب النطاق** — مناطق/تجمعات مستقلة الحساب يمكن توزيعها بأمان.
7. **Procedural/Lazy Generation** — تفاصيل تُولَّد عند الطلب فقط.

## قرارات مؤجَّلة عمدًا (ليست الآن)

- **Reflection/Semantic Type System**: نجرّب اعتماد قدرات Godot Resources/Export vars الجاهزة أولًا في أول Prototype قبل بناء نظام مخصص. أي "تصنيف دلالي" (Category, AI Importance...) إن احتجناه لاحقًا يوضع في ملف بيانات لعبة منفصل، لا في تعريف المحرك — هذا تصنيف Gameplay بطبيعته.
- **Dependency Graph التفاعلي الكامل (Reactive System)**: نعتمد بدلًا منه نسخة إعلانية خفيفة من اليوم الأول — قائمة بيانات بسيطة تسجّل "X يعتمد على Y" (تفيد أدوات التصحيح وترتيب التحميل)، بلا تنفيذ تلقائي (Reactive triggers). النسخة الكاملة (تفعيل تلقائي عند التغيير) تؤجَّل حتى تثبت الحاجة الفعلية بعد Prototype.
- **Modding API رسمي موثّق**: يتولّد لاحقًا من نظام الـ Plugin/Data الأساسي، لا حاجة لتصميمه بشكل منفصل الآن.
- **Git Strategy للفريق**: قرار عملية لا معمارية، يُبحث عند نمو الفريق فعليًا.

## Scenario Tests (نظام اختبار توازن آلي)

سكريبت يحمّل حالة محددة (دولتين، موارد معيّنة)، يشغّل عدد دورات محاكاة معروف، ويتحقق أن النتيجة داخل مدى متوقع. يعتمد كليًا على Determinism (بدونها النتائج غير قابلة للمقارنة). أولوية عالية — يبنى مبكرًا لحماية التوازن من الانكسار الصامت عند أي تعديل لاحق.

## معيار قبول عملي (يُختبر بعد أول Prototype)

إضافة دولة جديدة يجب ألا تتجاوز: `مجلد الدولة → country.json → flag.png → leader.json`. إن احتاج الأمر أكثر من ذلك بكثير، يُعاد النظر في التصميم.

## مشاريع مفتوحة المصدر — نقاط انطلاق مُقيَّمة

| المشروع | حالته | الدور المقترح |
|---|---|---|
| **SamTheBlow/grand-strategy-game** (Godot) | تطوير مبكر، أساسيات شغالة | الأساس المقترح للبناء فوقه — مرن، data-driven |
| **Project Alice** (C++, Victoria 2 remake) | Playable فعليًا | للتعلم — أفضل مثال حي لمحاكاة اقتصاد+سكان+سياسة |
| **OpenVic** (Godot, Victoria 2 remake) | لسه Unplayable | للتعلم من قرارات التصميم فقط، لا الكود |
| **dementive/gsg** (Godot 4.5 + C++23 + flecs ECS) | Prototype نشط | لفهم تطبيق ECS/الأداء عمليًا |

خطة مقترحة: دراسة Project Alice + dementive/gsg بالتوازي (الأول للمنطق، الثاني للأداء)، والبناء فوق grand-strategy-game كأساس.

## الخطوة التالية

بناء أول Prototype ملموس: Relevance Graph + Tiered Scheduler + معادلة قرار واحدة (مثل معادلة الانقلاب من وثيقة Gameplay Framework) كنموذج مصغّر شغّال فوق Godot، كإثبات مفهوم قبل التوسع.
