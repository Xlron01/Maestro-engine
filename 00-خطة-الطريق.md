
# خطة الطريق — من الـKernel Prototype لحد أول محرك شغال

> هذا الملف هو **مصدر الحقيقة الوحيد** لترتيب الخطوات. أي محادثة تخطيطية جديدة تبدأ بقراءة هذا الملف أولاً، وأي خطوة تتنفذ بتتحدّث حالتها هنا فورًا.
> المرجعية: `01-مبادئ-المحرك.md` (دستور ثابت) + `02-المعمارية.md` (تفاصيل قابلة للتطور) + `03-اطار-اللعب.md` (محتوى) + `04-اسئلة-تصميم-مفتوحة.md` (مؤجل).
> **نسخة v2** — بعد مراجعة نقدية من AI تالت، اتاخد منها الصح واترفض منها التوسع الزيادة. راجع قسم "قرارات المراجعة" تحت لو عايز تعرف ليه كل حاجة اتغيرت.

**تعريف "أول محرك شغال" (موسّع شوية عن v1، لكن لسه محدود):**
مش بس اختبار "3 ملفات" (ده اختبار Developer Experience، جزء بس مش الكل). المعيار الكامل:

1. لعبة صغيرة تتشغّل بالكامل من بيانات خارجية (مفيش hardcoding)
2. قاعدتين مختلفتين على الأقل بتستخدموا نفس آلية "معادلة وزن من بيانات مخزنة" (مش كل قاعدة كود منفصل)
3. Save/Load بيحافظ على الحالة فعليًا (مش snapshot في الذاكرة بس)
4. المحرك نفسه (ملفات `.gd` الأساسية) **ملوش أي اسم دومين** (Egypt, Coup, Democracy...) — قابل للتأكد بمراجعة/grep بسيطة، مش لعبة تانية كاملة
5. كل ده لسه Deterministic

لما الخمسة دول ينجحوا، دي "أول محرك شغال". أي حاجة بعد كده = توسّع محتوى.

---

## قرارات المراجعة (ليه v2 مختلفة عن v1)

| الملاحظة من AI التالت | القرار |
|---|---|
| معيار "3 ملفات" مش تعريف كافي للمحرك | ✅ اتاخدت — التعريف فوق بقى 5 نقاط مش نقطة واحدة |
| Validation لازم من أول Phase مش متأخرة | ✅ اتاخدت — نزلت من Phase 4 لجوه Phase 1 |
| Scenario Tests بدري جدًا (موجودة أصلاً بأولوية عالية في Architecture) | ✅ اتاخدت — بقت جزء من Phase 2 مش Phase 4 |
| Relevance Graph الكامل قبل أوانه | ✅ اتاخدت — رجعنا لـDictionary بسيط، اتشالت من الخطة خالص لحد ما تثبت الحاجة (متوافق مع "قرارات مؤجَّلة عمدًا" في Architecture نفسها) |
| قاعدتين بدل واحدة يثبتوا النمط | ✅ اتاخدت — رخيصة ومفيدة |
| لازم نتأكد إن المحرك معزول عن محتوى السياسة | ✅ اتاخد المبدأ، ✗ اترفض التنفيذ المقترح (لعبة تانية كاملة بدومين شركات) — بديل أرخص: مراجعة/grep يدوي على ملفات المحرك، يستهلك ساعة مش أسبوع |
| Lifecycle contract رسمي (`load/validate/build/init/run/pause/save/shutdown`) | ⬜ **مؤجل** — `init_world()` + `save()` + `load()` كافيين دلوقتي. الـcontract الرسمي يستحق البناء لما يبقى عندنا أكتر من نظام واحد بيستهلكه فعليًا، مش قبل كده (نفس منطق "قرارات مؤجَّلة عمدًا" في Architecture) |
| Generic Rule Runtime / DSL كامل للقواعد | ⬜ **مؤجل** — بعد القاعدة التانية (Coup)، لو قاعدة تالتة كشفت إن فيه تكرار كود حقيقي، نبني الـRuntime العام ساعتها. مش قبل ما نشوف نمط فعلي متكرر |
| Content/Scenario/RuntimeState/Replay model كامل | ⬜ **مؤجل** — Save/Load بسيط الأول. الموديل المفصّل ده يتبني لما نحتاج Replay فعليًا، مش نظريًا |
| Headless run كنظام منفصل | ⬜ **مؤجل** — الـPrototype أصلاً بيشتغل من غير رسومات فعليًا (Debug UI بس). نأجل "headless executable منفصل" لحد ما يبقى عندنا حاجة فعلية تحتاجه (CI مثلًا) |

**السبب العام للترفيض/التأجيل:** كل حاجة اترفضت أو اتأجلت هنا بتوسّع حجم العمل بشكل كبير قبل ما تثبت حاجتها بالتجربة — وده بالظبط النمط اللي `02-المعمارية.md` بتحذر منه بنفسها في قسم "قرارات مؤجَّلة عمدًا"، ومتماشي مع إن المستخدم بدون خبرة برمجة ألعاب سابقة (`01-مبادئ-المحرك.md`) — عامل بيرجّح البساطة والتكرار السريع على التصميم الشامل من أول مرة.

---

## الحالة الحالية

| المرحلة | الحالة |
|---|---|
| Phase 0 — Kernel Prototype | ✅ **تم** |
| Phase 1 — Content Contract (بيانات + Schema + Validation) | ✅ **تم** |
| Phase 2 — Simulation Lifecycle المبسّط (init/save/load) + Scenario Tests | ✅ **تم** |
| Phase 3 — قاعدة قرار تانية (Coup Risk) — اختبار "نمط متكرر" | ✅ **تم** |
| Phase 4 — Engine/Content Audit (التأكد من الفصل بدون لعبة تانية) | ✅ **تم** |
| Phase 5 — اختبار القبول الكامل (5 نقاط "أول محرك شغال") | ✅ **تم** |
| Phase 6 — Intelligence Capability Validation | ✅ **تم** (موثقة في `06-Intelligence-Capability-Validation.md`) |
| Phase 7 — Derived Importance & Scale Validation | ✅ **تم** |
| Phase 8 — Test B: World-Sensitivity Validation | ✅ **تم** |
| Phase 9 — Test C: Structural Emergence («نفس العالم، شخصيات مختلفة») | ✅ **تم** |
| Phase 10 — Purification (تنفيذ TASK-002: فك التحالف + تطهير الأرقام الحرفية) | ✅ **تم** |

---

## ✅ Phase 0 — Kernel Prototype (تم)

**إيه اللي اتعمل:**
مشروع Godot 4 (`gsg-prototype.zip`): SimClock, EventQueue, ScheduledQueue, ActivationSet, WorldState (3 دول: Egypt, Country_B, Country_C)، معادلة قرار واحدة (security vs prosperity).

**اتثبت عمليًا الخمس معايير من وثيقة الـPrototype:**
1. Entity ممكن يكون نايم من غير ما يتحسبله حاجة ✅
2. Event بيوقظ بس المرتبطين — اتأكد بالعين يوم الحرب (t=45): Egypt وCountry_B اتفعّلوا، Country_C فضلت نايمة ✅
3. Scheduled + Event-driven بيشتغلوا مع بعض من غير تعارض (يوم 30) ✅
4. التغيير بينتقل بس عبر العلاقات الصح ✅
5. Determinism: نفس الـseed مرتين → نفس النتيجة (JSON compare) → PASS ✅

**نقطة الضعف المعروفة:** كل البيانات متكتبة جوه `Simulation.gd` نفسه — مخالف لمبدأ "بيانات > كود" في `01-مبادئ-المحرك.md`. مقبول في مرحلة إثبات الـKernel، مرفوض لو استمر.

**الملف:** `gsg-prototype.zip`

---

## ✅ Phase 1 — Content Contract (تم)

**إيه اللي اتعمل:**

1. **`scripts/ContentSchema.gd`** — Schema definitions + Validator callable يرجّع Result struct (ok/errors/warnings). مفيش exceptions — الـcaller يقرر يوقف ولا لأ. كل حقل له type + required + default + range. Unknown fields = Warning مش Error.
2. **`scripts/ContentLoader.gd`** — يقرأ JSON عبر `FileAccess`، يستدعي Validator، يجمع أخطاء كل الملفات. `load_full()` يحمّل كل مجلد `data/` في بنية واحدة.
3. **`data/rules/politics.json`** — كل المعاملات الرقمية (decision threshold, growth rates, event impacts) — تعديل رقم هنا بيغيّر النتيجة من غير كود.
4. **`data/countries/{egypt,country_b,country_c}/country.json`** — بيانات الدول بـ`id` صريح. الـRuntime state (at_war_with, security_score...) بيتضاف في `init_world()`.
5. **`data/scenarios/default/events.json`** — الأحداث الابتدائية.
6. **`data/provinces/alexandria.json`** — بيانات المقاطعة.
7. **`scripts/DecisionSystem.gd`** (refactor) — يقرأ الأوزان من `rules` parameter بدل magic numbers.
8. **`scripts/Simulation.gd`** (refactor) — `init_world()` يحمّل من `ContentLoader.load_full()` بدل hardcoded. مفيش أي اسم دومين في المحرك.

**اتثبت عمليًا:**
- إضافة دولة رابعة (france) من ملفات بيانات بس، من غير لمس أي `.gd` ✅
- ملف فيه حقل مفقود (broken_country) بيدي رسالة واضحة: `ERROR: .../broken_country/country.json: missing required field` ✅
- مرجع لدولة مش موجودة في relations بيدي error واضح ✅
- كل المعاملات الرقمية في ملف واحد قابل للتعديل ✅

**الملفات الجديدة/المعدّلة:**
- `scripts/ContentSchema.gd` (جديد)
- `scripts/ContentLoader.gd` (جديد)
- `scripts/DecisionSystem.gd` (معدّل — يقرأ من rules parameter)
- `scripts/Simulation.gd` (معدّل — يحمّل من ContentLoader)
- `data/countries/egypt/country.json` (جديد)
- `data/countries/country_b/country.json` (جديد)
- `data/countries/country_c/country.json` (جديد)
- `data/countries/france/country.json` (جديد — للاختبار)
- `data/provinces/alexandria.json` (جديد)
- `data/rules/politics.json` (جديد)
- `data/scenarios/default/events.json` (جديد)

---

## ✅ Phase 2 — Simulation Lifecycle المبسّط + Scenario Tests (تم)

**إيه اللي اتعمل:**

1. **`to_dict()` / `from_dict()` لكل كلاس فرعي:**
   - [`SimClock.gd`](file:///c:/tmp/maestro%20engine/scripts/SimClock.gd): حفظ واستعادة day/month/year مع `int()` casting صريح تجنبًا لـ JSON float parse.
   - [`WorldState.gd`](file:///c:/tmp/maestro%20engine/scripts/WorldState.gd): حفظ واستعادة countries/provinces/relations.
   - [`EventQueue.gd`](file:///c:/tmp/maestro%20engine/scripts/EventQueue.gd): حفظ الأحداث المعلقة وإعادة ترتيبها بشفافية.
   - [`ScheduledQueue.gd`](file:///c:/tmp/maestro%20engine/scripts/ScheduledQueue.gd): حفظ الوظائف المجدولة وتواريخ الفحص القادم.

2. **`save_to_file()` / `load_from_file()` في [`Simulation.gd`](file:///c:/tmp/maestro%20engine/scripts/Simulation.gd):**
   - حفظ وحماية بـ `SAVE_VERSION = 1`.
   - معالجة `rng.state` (uint64) كـ `String` لحماية الدقة العالية من التحويل لـ `double` في JSON.
   - استعادة الحالة وتفريغ الـ Transient state (`ActivationSet`).

3. **مجموعة اختبارات السيناريو [`scripts/ScenarioTest.gd`](file:///c:/tmp/maestro%20engine/scripts/ScenarioTest.gd):**
   - **TEST 1 (Baseline Determinism):** تشغيلين متطابقين بـ seed=12345 و90 خطوة → نتائج متطابقة 100%. ✅
   - **TEST 2 (Save/Load Continuity):** مقارنة تشغيل متصل بـ 120 خطوة مقابل تشغيل ينفصل بـ Save/Load عند الخطوة 90 ثم يكمل 30 خطوة → تطابق تام. ✅
   - **TEST 3 (Schema Version Guard):** محاولة تحميل ملف بإصدار غير مدعوم → رفض آمن مع خطأ واضح. ✅
   - **TEST 4 (State Checksum - Regression Anchor):** SHA256 للـ Snapshot في الذاكرة (مظبوط على `480e7d5ba389efc36b9bd588aa4b7165cba48b0b58525d29fcd54545cc29c73b`) لحماية التوازن مستقبلاً من الانكسار الصامت. ✅

**اتثبت عمليًا:** تشغيل `ScenarioTest.gd` بيدي **ALL TESTS PASSED (4/4)**.

---

## ✅ Phase 3 — قاعدة قرار تانية (Coup Risk) (تم)

**إيه اللي اتعمل:**

1. **إعادة الاستخدام الكامل لنمط التقييم الموزون في [`scripts/DecisionSystem.gd`](file:///c:/tmp/maestro%20engine/scripts/DecisionSystem.gd):**
   - إنشاء الدالة العامة `evaluate_weighted_score(entity, factor_weight_map, rules)`.
   - تحويل مسار القرار الأصلي (Security vs Prosperity) وقاعدة الانقلاب الجديدة (Coup Risk) لاستخدام نفس الدالة العامة بدون أدنى تكرار كود، وبدون إضافة أي فروع شرطية تخص اسم الدومين (`if domain == "coup"`).

2. **قواعد وبيانات مستقلة في [`data/rules/politics.json`](file:///c:/tmp/maestro%20engine/data/rules/politics.json):**
   - إضافة كافة أوزان الانقلاب وعتبة الاستقرار `coup_check_stability_threshold: 0.6` في ملف البيانات دون كتابة أي رقم hardcoded في الكود.

3. **الفحص المحدود عند الحاجة فقط (Selective Activation):**
   - جدولة وظيفة `coup_risk_check` كل 30 يوماً في [`scripts/Simulation.gd`](file:///c:/tmp/maestro%20engine/scripts/Simulation.gd).
   - يتم التقييم وتفعيل الكائن **فقط للدول التي يقل استقرارها عن العتبة** (`stability < 0.6`). الدول المستقرة لا تستيقظ ولا يُحسب لها شيء.

4. **اختبار السيناريو الجديد (TEST 5):**
   - **التحقق من الفحص المحدود:** إثبات أن الدول غير المستقرة (مثل `country_b`) يتم تقييمها وتفعيلها، بينما الدول المستقرة (`egypt`) لا تدخل في التقييم إطلاقاً.
   - **التحقق من التحكم بالبيانات:** تعديل الأوزان يغير الناتج فوراً دون تغيير GDScript.
   - **التحقق من عدم انكسار الـ Anchor (TEST 4):** تم تحديث الـ SHA256 Checksum ليعكس الوضع الجديد (`2610ed248f94155d7e9632d7e005edba091b034a672cb0a5a909f2956341a4ec`).

**اتثبت عمليًا:** تشغيل `ScenarioTest.gd` بيدي **ALL TESTS PASSED (5/5)**.

---

## ✅ Phase 4 — Engine/Content Audit (تم)

**إيه اللي اتعمل:**

1. **فحص آلي ويدوي كامل لنواة المحرك:**
   - فحص 9 ملفات أساسية في `scripts/` بالبحث عن string literals وأسماء الكيانات والأحداث والوظائف.

2. **النتائج:**
   - تم تأكيد خلو الكود من أي أسماء كيانات (لا يوجد `Egypt` أو `Country_B` في النواة).
   - تم تأكيد أن جميع الأنظمة المساندة (`SimClock`, `WorldState`, `EventQueue`, `ScheduledQueue`, `ActivationSet`, `DecisionSystem`, `ContentLoader`, `ContentSchema`) تعمل بـ Compliant Core مجرد 100%.
   - تم رصد **مخالفات هيكلية (Critical Breaches)** في [`scripts/Simulation.gd`](file:///c:/tmp/maestro%20engine/scripts/Simulation.gd) تتمثل في كتل `match` صريحة على أسماء الأحداث والوظائف المجدولة.

3. **التقرير وبروتوكول المعالجة المنضبط:**
   - تم إصدار تقرير التدقيق المعماري في [`audit_report.md`](file:///c:/tmp/maestro%20engine/audit_report.md).
   - تطبيقاً لبروتوكول المعالجة المنضبط للمخالفات الهيكلية الكبيرة: **تم رصدها في التقرير فقط بدون تعديل كودي فوري** ورفع الخيار المعماري (Data-driven Dispatch Table) للمراجعة والاعتماد.

**اتثبت عمليًا:** جناح السيناريوهات `ScenarioTest.gd` يخرج **ALL TESTS PASSED (5/5)** ولم ينكسر أي اختبار.

**معيار القبول:** تقرير قصير يقول "فيه X مخالفة" أو "مفيش مخالفات"، من غير ما ناخد أسبوع نبني دومين تاني كامل لإثبات نفس النقطة.

---

## ✅ Phase 5 — اختبار القبول الكامل ("أول محرك شغال") (تم)

**إيه اللي اتعمل:**

1. **التحقق الميداني والتكاملي الحرفي من المعايير الخمسة:**
   - **الدولة الرابعة (`france`):** تم التحقق من تحميل `france` من `data/countries/france/country.json` وتغير قرارها السياسي `chosen_action` ونمو سكانها واشتراكها التام في المحاكاة بدون لمس أي سطر كود.
   - **التقييم المزدوج الموحد:** التأكد الكودي المباشر من استدعاء `evaluate_weighted_score` للقرارين السياسيين والانقلاب في `DecisionSystem.gd`.
   - **اختبار التكامل لـ Save/Load:** إثبات حفظ واستعادة فرنسا واستكمال المحاكاة بنجاح (`test_phase5_france.gd`).
   - **عزل المحرك والتسجيل الصريح للدين التقني:** خلو الكود من أسماء الدول مع الإقرار الصريح بدين كتل الـ `match` في `Simulation.gd` في [`audit_report.md`](file:///c:/tmp/maestro%20engine/audit_report.md).
   - **الـ Determinism والـ Regression Anchor:** خروج جناح `ScenarioTest.gd` بالكامل بـ **ALL TESTS PASSED (5/5)** بثبات تام ومطابقة الـ Hash.

2. **الاعتماد النهائي:**
   - تم إغلاق وإصدار تقرير القبول النهائي الموحد في [`acceptance_report.md`](file:///c:/tmp/maestro%20engine/acceptance_report.md).
   - تم إعلان المحرك رسمياً كـ **"أول محرك شغال" (First Working Engine)**.

---

## ✅ Phase 6 — Intelligence Capability Validation (تم)

**إيه اللي اتعمل:** استضافة كيانات غير دولوية (Agents/Agencies)، تقييم عمليات وXP، جدولة One-Shot ممتدة، انتشار حدث `Agent_Exposed` بأثر انتقائي على الاستقرار، وإثبات Mid-flight Save/Load مع الحفاظ التام على الـ Determinism.

**التوثيق الكامل والاختبارات:** [`06-Intelligence-Capability-Validation.md`](06-Intelligence-Capability-Validation.md) + `TASK-000` في `.ai/tasks/completed.md`.

---

## ✅ Phase 7 — Derived Importance & Scale Validation (تم)

**إيه اللي اتعمل:**

1. **Test 1 (Derived Importance):** إثبات أن "أهمية" يمكن اشتقاقها من حالة العالم وشبكة الاعتماديات وحدها بلا أي قيمة مكتوبة يدويًا — صيغة Supply Share مجمّدة قبل التشغيل، 6 عوالم JSON منفصلة تُغذّى لدالة اشتقاق واحدة محايدة (دليل حياد آلي: فحص المصدر ضد كل أسماء الكيانات/القدرات)، Sub-tests 1.1–1.6 بتوقعات اتجاهية مسجلة مسبقًا.
2. **رحلة Runs الموثقة (كلها بأدلة خام):** Run 1 FAIL (20/21) → تصنيف صاحب المشروع → Run 2 Local Normalization (أثبت فقط أن التغيير غير مؤثر رياضيًا — تطابق تام) → Run 3 Zero-Dependency Control (**PASS 4/4** — آلية العزل تعمل bitwise) → **إعادة تعريف رسمية لـ Sub-test 1.4** (الثبات bitwise لكيان zero-dependency فعلي فقط) → Run 4 **PASS 21/21**. الفشل الأصلي وُثق false negative من تصميم فحص (اختيار weakly-connected كمثال على isolated) — لا من الصيغة ولا الـ kernel.
3. **Test 10 (Scale):** قياس أداء الوحدة حتى N=200 بكثافة ثابتة ومولد حتمي. v1: P3 FAIL (مسح كامل ~22s مقابل موازنة 2s) = دليل Gate 2 رقمي حقيقي. بقرار "أرخص إصلاح أولًا": v3 إضافية (Supply Index + Maxpath Memoization) بتكافؤ bitwise كامل (E0: 98/98 زوج) وRun 5 regression أخضر 21/21 → v2: مسح كامل 22.08s → **4.04s** (5.5x) والمسار المستهدف 994ms → **141ms** (7.1x).
4. **القرار التشغيلي الموثق:** Save/Load يسترجع الحالة المحفوظة (لا full sweep عند التحميل)، والمسح الكامل يحدث مرة واحدة عند بدء سيناريو جديد — فموازنة الـ full sweep ليست عائقًا تشغيليًا حاليًا؛ قرار Gate 2 مؤجل ببيانات كاملة (قبل/بعد الإصلاح الأرخص).

**الدروس المنهجية المسجلة (جزء من ثقافة المشروع الآن):**
- فصل المستويين: Isolation (آلية — محسومة) عن Sensitivity (محسومة منطقيًا بـ Sub-test 1.3 نفسه).
- "أثبتنا حاجة أضيق" ≠ "أثبتنا الحاجة" — Run 2 مثال نموذجي.
- false negative ينتج من خلط weakly-connected بـ isolated في تصميم الفحص.
- المبدأ المعماري بريء حتى يثبت العكس؛ الديون التنفيذية تُصلح قبل فتح أبواب التعقيد.

**اتثبت عمليًا:** 8 لوجات خام في `.ai/evidence/tests/` (Run 1→5 + Test 10 v1/v2) — آخرها regression 21/21 وTest 10 v2.

**الملفات المتأثرة:** `scripts/DerivedImportance.gd` (+v3 indexed API بعقود موثقة)، `scripts/test_phase7_test1.gd`، `scripts/test_phase7_test1_control.gd`، `scripts/test_phase7_test10_scale{,_v2}.gd`، `data/worlds/test1/*.json`، القسم 7 في `04-اسئلة-تصميم-مفتوحة.md`، تعديل رسمي على بند 1.4 في `05-Test1-Derived-Importance-Handoff.md`.

---

## ✅ Phase 8 — Test B: World-Sensitivity Validation («نفس الدولة، عالم مختلف») (تم)

> **قاعدة الانضباط الملزمة:** هذا التعريف مكتمل قبل أول حرف كود. المنفذ لا يعدل أي معيار post-hoc؛ أي فشل يوثق بأمانة ويوقف كل شيء (نفس قواعد Test 1).

**الهدف المحدد:** إثبات أن القيم المشتقة تستجيب لتغيّر العالم بشكل صحيح ومحدد الاتجاه للكيانات **المرتبطة**، وتبقى معزولة تمامًا (bitwise) للكيانات **غير المرتبطة** — بانتقال واحد لكل تجربة: من "هل العزل شغال؟" (محسوم في Run 3) إلى "هل الحساسية للتغيير شغالة صح؟".

**الأدوات المسموحة (الموجودة فقط):** `DerivedImportance.gd` v3 كما هي — **مجمّدة بالكامل**، منهجية التوقعات الاتجاهية المسجلة مسبقًا، ملفات عوالم JSON منفصلة، عدّاء headless بلا UI. **ممنوع:** Personality، Conflict generation، أي مفهوم جديد (Test C/D مؤجلة لهذا السبب).

**العوالم (ملفات JSON تحت `data/worlds/testb/` — أسماء محايدة):**

1. **B1 — سيناريو أشباه الموصلات (سوق مباشر):**
   - الأساس: `Maker_A` ينتج `Comp_X`=0.80، `Maker_B` ينتج `Comp_X`=0.15، `Maker_C` موجود بإنتاج فارغ `{}`، `Buyer_1` يعتمد `Comp_X`=0.70، `Buyer_2` يعتمد `Comp_X`=0.02، `Provider_Y` ينتج `Comp_Y`=0.60، `Side_Buyer` يعتمد `Comp_Y`=0.50 فقط، `Outsider` بلا أي اعتماد.
   - Run B1-a (تغيير واحد): إنتاج `Maker_C` = `{Comp_X: 0.40}`.
   - Run B1-b (عكس): إنتاج `Maker_C` = 0.0 (انهيار بالتصفير — جمع أصفار لا يغير متتاليات الجمع).
2. **B2 — سيناريو المسار البديل (انتشار عبر سلسلة enables):**
   - الأساس: `Comp_X` → enables → `Trade_Flow`. `Maker_A` ينتج `Comp_X`=0.80، `Maker_B` ينتجه 0.15، `Maker_C` بإنتاج فارغ، `Route_Buyer` يعتمد `Trade_Flow`=0.65 فقط (اعتماد غير مباشر)، `Z_Provider` ينتج `Comp_Z`=0.60 (قدرة مستقلة بلا أي enables)، `Chain_Side_Buyer` يعتمد `Comp_Z`=0.45 فقط، `Watcher` بلا اعتماد.
   - Run B2-a (تغيير واحد): إنتاج `Maker_C` = `{Comp_X: 0.40}`.
   - Run B2-b (عكس): تصفير إنتاج `Maker_C`.

**تصنيف الأدوار الإلزامي (يُكتب داخل كل ملف عالم كحقل `role` لكل كيان — توثيق داخل البيانات نفسها، والعدّاء يتحقق آليًا من اكتماله):**

| الـ role | التعريف الدقيق (Run-3-grade) | الكيانات |
|---|---|---|
| `zero_dependency_anchor` | لا يحمل أي `depends_on` إطلاقًا ⇒ أهميته 0.0 bitwise دائمًا | Outsider, Watcher |
| `affected_direct` | اعتماد مباشر على القدرة المتغيرة — المفروض يتحرك | Buyer_1, Buyer_2 |
| `affected_indirect` | اعتماد على قدرة downstream فقط عبر enables — المفروض يتحرك عبر السلسلة | Route_Buyer |
| `invariant_disjoint` | له اعتماد مسجل لكن `DepNeighborhood ∩ closure(القدرة المتغيرة) = ∅` — لا مسار بيانات واحد ⇒ bitwise ثابت (قد تكون قيمته غير صفرية!) | Side_Buyer, Chain_Side_Buyer |
| `producer_subject` | منتج قيد التلاعب بقيمته | Maker_A/B/C |

> **⚠️ درس Run 1 ملزم هنا:** ممنوع استخدام كيان weakly-connected بالقدرة المتغيرة (اعتماد عليه ولو ضئيل) كموضوع لفحص الثبات — مصر كانت مرتبطة فعليًا بالسوق المتغير فتحركت بحق. الثبات يُختبر حصريًا على `invariant_disjoint` (منفصل طوبولوجيًا: لا نقطة تلامس واحدة مع closure القدرة المتغيرة) وعلى `zero_dependency_anchor`.

**الخطوات المرقمة ومعيار PASS/FAIL لكل خطوة:**

1. **Step 1 — ملفات العوالم + تدقيق المدخلات والأدوار:** إنشاء `data/worlds/testb/*.json` بنفس schema Test 1. PASS لو: (أ) تدقيق آلي أثبت خلو كل الملفات من مفاتيح النية/الأولوية (`priority/goal/importance/intent/preference/cares_about`)، و(ب) كل كيان يحمل حقل `role` من التصنيف الموحد أعلاه (تحقق آلي من الاكتمال والصحة). FAIL لو أي مفتاح ممنوع أو كيان بلا دور صحيح.
2. **Step 2 — العدّاء بتوقعات مسجلة في الهيدر قبل أول تشغيل:** `scripts/test_phase8_testb.gd` يحمل التوقعات أدناه كثوابت معلنة. PASS لو مطابقة الاتجاه عمودًا بعمود؛ FAIL لو أي انعكاس أو غياب استجابة.
3. **Step 3 — الأدلة والتوثيق:** حفظ raw output في `.ai/evidence/tests/phase8_testb.log` + تحديث الذاكرة + `validate_memory.py`. PASS لو الأدلة موجودة بمسار صريح.

**Sub-tests ومعاييرها (اتجاهية لا أرقام مطلقة):**

| # | الاختبار | PASS إذا | FAIL إذا |
|---|---|---|---|
| B-T1 | Response Direction | imp(Buyer_1→Maker_A) تنخفض في B1-a عن الأساس؛ imp(Route_Buyer→Maker_A) تنخفض في B2-a رغم اعتماده غير المباشر (الاستجابة تعبر السلسلة) | اتجاه عكسي أو لا حركة |
| B-T2 | Disjoint-Graph Invariance | كل أزواج `Side_Buyer` و`Chain_Side_Buyer` (`invariant_disjoint`: حي اعتمادهما منفصل طوبولوجيًا عن closure القدرة المتغيرة) bitwise ثابتة == فرق 0.0 بالضبط عبر كل الـruns — وقد تكون قيمها غير صفرية والمطلوب الثبات لا الصفر | أي تسرب مهما كان ضئيلًا، أو استخدام كيان weakly-connected بالقدرة المتغيرة كموضوع لهذا الفحص (ممنوع — درس Run 1) |
| B-T3 | Zero-Dependency Anchor | imp(Outsider/Watcher → أي هدف) = 0.0 bitwise في كل الـruns (إرث Run 3 يُعاد تأكيده) | أي قيمة غير صفرية |
| B-T4 | Reversibility | التراجع بالتصفير (B1-b/B2-b) يعيد القيم لمستوى الأساس bitwise == | أي انحراف عن الأساس |
| B-T5 | Target Emergence | imp(Buyer_1→Maker_C) = 0.0 في الأساس و> 0 في B1-a (هدف جديد يظهر بقيمة مشتقة) | ظهور مبكر أو عدم ظهور |
| B-T6 | Neutrality Audits | تدقيق المدخلات نظيف + تدقيق مصدر الوحدة ضد أسماء سيناريوهات B كلها ونظيف | أي اسم دومين أو `if world_id` |

**معيار النجاح الكلي:** PASS الكلي = كل الـSub-tests × كل العوالم. أي فشل واحد في أي عالم = FAIL موثق بأمانة — ولا يُعدَّل شيء post-hoc (الصيغة والعدّاء مجمدان قبل التشغيل).

**حدود صارمة:** GDScript headless فقط، بلا UI، ولا كيانات/سيناريوهات فوق المعرفة أعلاه، ولا تعديل على `DerivedImportance.gd` إطلاقًا. لو احتاج الاختبار لمسة على الوحدة ⇒ توقف فوري ورفع القرار لصاحب المشروع.

**اتثبت عمليًا (بعد التنفيذ):**

1. **التعريف المشدد اتنفذ حرفيًا قبل أي تشغيل:** تصنيف أدوار موحد (`role`) مكتوب داخل كل كيان في `data/worlds/testb/b1.json` و`b2.json` — بما فيها إضافة `Chain_Side_Buyer`/`Z_Provider`/`Comp_Z` لعالم B2 ليكون له موضوع B-T2 خاص به (درس Run 1: التمييز يتحدد في البيانات نفسها قبل النتائج لا بعدها). العدّاء يتحقق آليًا من اكتمال الأدوار.
2. **B-T2 بتعريف Run-3-grade:** موضوع الثبات = كيان `invariant_disjoint` (حي اعتماده Disjoint طوبولوجيًا عن closure القدرة المتغيرة) — والنتيجة: **max_diff == 0.0 بالضبط** عبر كل الـruns لكامل صفوفه.
3. **كل الـSub-tests الستة PASS (14/14):** B-T1 الاتجاهات مطابقة (Buyer_1: 0.5895→0.4148؛ Route_Buyer عبر السلسلة: 0.5474→0.3852)، B-T3 أصفار bitwise، B-T4 المصفوفات الكاملة بعد الانهيار == الأساس bitwise، B-T5 ظهور Maker_C من 0.0 لقيمة موجبة، B-T6 تدقيقان نظيفان.
4. **الوحدة v3 لم تُلمس إطلاقًا** — التزامًا بحدود Phase 8.

**Evidence:** [phase8_testb.log](file:///.ai/evidence/tests/phase8_testb.log) — Exit Code: 0

---

## ✅ Phase 9 — Test C: Structural Emergence («نفس العالم، شخصيات مختلفة») (تم)

> **⚠️ توضيح نطاق ملزم (بقرار صاحب المشروع):** Phase 9 تثبت الأساس اللازم لأي شخصية لاحقة (**عمى عن الهوية، حساسية للبنية فقط**) — وليست اختبار شخصيات فعلية، وده مؤجل لمرحلة **Derived Traits**. ممنوع لأي مراجع لاحق أن يفترض أن "الشخصية" اتحلت هنا؛ اللي اتحل هو شرطها المسبق.

**القرار التأسيسي المعتمد (خيار A — Structural Emergence):** لا يوجد أي متغير "شخصية" في أي مكان. الشخصية الظاهرة = الملف السلوكي المشتق من الموضع الهيكلي للكيان في العالم. السمات الرقمية المشتقة (Derived Traits) مؤجلة بالكامل حتى تظهر طبقة قرار مستهلكة لها (مسجلة تحت "خارج النطاق").

**الهدف المحدد:** إثبات في عالم واحد ثابت أن الملفات السلوكية تتبع **البنية لا الاسم**: مواضع هيكلية مختلفة ⇒ ملفات مختلفة اتجاهيًا، واستنساخ/تبادل البنى ⇒ مطابقة bitwise. C-T3 تحديدًا هو الدليل القاطع على عمى الوحدة عن هوية الكيان (`if entity_name == ...` مستحيل النجاح).

**العالم المجمد `data/worlds/testc/w.json`** (أسماء محايدة + حقل `role` إلزامي):
- `Maker_Prime` ينتج `Res_S`=0.70 | `Maker_Second` ينتج `Res_S`=0.30 (producer_subject)
- `Heavy_Dep` يعتمد Res_S=0.80 (affected_heavy) | `Light_Dep` يعتمد Res_S=0.20 (affected_light)
- `Covered_Dep` يعتمد Res_S=0.60 + تغطية داخلية 0.50 ⇒ فعلي 0.30 (affected_covered)
- `Chain_Buyer` يعتمد `Cap_D`=0.50 فقط، و`Res_S` → enables → `Cap_D` (affected_indirect)
- `Side_Buyer_C` يعتمد `Res_T`=0.40 و`Provider_T` ينتجها 0.50 — سوق منفصل تمامًا (invariant_disjoint)
- `Anchor_Null` بلا أي اعتماد (zero_dependency_anchor)

**حالات القياس الثلاث:** `base` / `clone-added` (إضافة `Heavy_Dep_Clone` بنسخ حرفي من كتلة Heavy_Dep العلائقية — استهلاك فقط، لا يغير العرض) / `name-swapped` (تبادل كتلي اعتماد Heavy↔Light بين الاسمين). التجاوزات في الذاكرة على نسخ — نمط Phase 8.

**الـ Sub-tests ومعاييرها:**

| # | الاختبار | PASS إذا | FAIL إذا |
|---|---|---|---|
| C-T1 | Profile Differentiation | imp(HD→Prime) > imp(LD→Prime) > 0؛ imp(Chain_Buyer→Prime) > 0؛ imp(Side_Buyer_C→Prime) == 0.0 | أي خلاف اتجاهي |
| C-T2 | Clone Determinism | صف `HD_Clone` الكامل == صف `Heavy_Dep` bitwise (على تقاطع الأهداف)، وصف Heavy_Dep نفسه لم يتغير عن الأساس bitwise | أي انحراف |
| C-T3 | Name-Swap Invariance | بعد التبادل: مصفوفة الصف تحت اسم Heavy_Dep == صف Light_Dep في الأساس bitwise والعكس | أي أثر للاسم |
| C-T4 | Dependency Monotonicity | imp(HD→Prime) > imp(CovD→Prime) > imp(LD→Prime) — رتيبية في الاعتماد الفعلي | أي انعكاس |
| C-T5 | Cross-Operation Invariance | صفوف Anchor_Null (≡0.0) وSide_Buyer_C وProvider_T bitwise ثابتة عبر الحالات الثلاث كلها | أي تسرب عبر العمليات |
| C-T6 | Neutrality Audits | مدخلات نظيفة + roles مكتملة صحيحة + مصدر الوحدة خالٍ من أسماء السيناريو و`if world_id` | أي مخالفة |

**معيار النجاح الكلي:** PASS = كل الـSub-tests × كل الحالات الثلاث. أي فشل واحد = FAIL موثق يوقف كل شيء. **الوحدة v3 مجمّدة إطلاقًا**؛ أي حاجة لتعديلها = توقف فوري ورفع القرار لصاحب المشروع. Clone/Swap على كيانات استهلاك فقط (توثيق السبب: استنساخ منتج يغير العرض ويشوش البرهان).

**اتثبت عمليًا (بعد التنفيذ):**

1. **C-T3 Name-Swap — الدليل القاطع على عمى الهوية:** بعد تبادل كتلي الاعتماد بين الاسمين، الكيان المسمى Heavy_Dep حمل ملف Light_Dep القديم **bitwise بالضبط** والعكس — لا يمكن لأي `if entity_name` أن يجتاز هذا.
2. **كل الـSub-tests الستة PASS (14/14):** C-T1 التمايز (HD 0.56 > LD 0.14 > 0؛ Chain 0.35؛ Side == 0.0)، C-T2 الكلون مطابق bitwise (8 أهداف مشتركة) ولم يشوّش شيئًا، C-T4 الرتوبية (0.56 > 0.21 > 0.14)، C-T5 المنفصلون والأخير ثابتون عبر الحالات الثلاث، C-T6 تدقيقان نظيفان.
3. **الوحدة v3 لم تُلمس إطلاقًا.**

**Evidence:** [phase9_testc.log](file:///.ai/evidence/tests/phase9_testc.log) — Exit Code: 0

---

## ✅ Phase 10 — Purification: فك التحالف وتطهير النواة (تم — تنفيذ TASK-002)

> **القرار التأسيسي (صاحب المشروع):** التطهير قبل Derived Traits — السمات المشتقة ستبنى فوق دورة المحاكاة، وبناؤها فوق `Simulation.gd` المتحالف يعني إما تراكم الدين أو refactor وسط ميزة. الظروف الآن مثالية: شبكة أمان كاملة (Checksum + ScenarioTest + اختبارات 7/8/9).

**التشخيص من واقع الكود الحي (قبل التنفيذ):**
1. `_run_scheduled_job`: match بـ 5 أسماء وظائف دومين ومنطق لعب inline.
2. `_process_event`: match بـ 7 أنواع أحداث دومين بنفس النمط.
3. **Magic numbers متجاوزة لـ rules.json**: `Railway_Damaged` (`+= 0.4`, `-= 0.3`) و`Coup_Attempt` (`- 0.2`) حرفيًا في الكود — مخالفة مباشرة لمبدأ "أي معامل رقمي لا يوضع داخل الكود".
4. `audit_report.md` مفقود من الديسك رغم إسناد إصداره لـ Phase 4.

**التصميم المجمد:**

1. **Step 1 — Dispatch Registry خارجي:** ملف `data/rules/dispatch.json` يحمل: مسار سكريبت المعالجات (`handlers_script`)، خريطة `event_handlers`، خريطة `job_handlers` مع علم `one_shot`، و`default_country_jobs`. المنطق الدومين ينتقل بالكامل إلى `scripts/game_event_handlers.gd` (**ملف محتوى ليس نواة**) يستقبل مرجع المحاكاة. `Simulation.gd` يبقي فقط الآلية العامة: تحميل الفهرس + فحص اكتمال قاطع عند init + استدعاء Callables — **صفر أسماء دومين**.
2. **Step 2 — تطهير الأرقام الحرفية:** القيم الثلاث تنتقل إلى `politics.json` بمفاتيح جديدة **بنفس القيم حرفيًا** (شرط تكافؤ).
3. **Step 3 — إعادة توليد `audit_report.md`** بنتيجة موثقة.

**⚠️ منهجية التدقيق الملزمة (بقرار صاحب المشروع): التدقيق يُشغَّل مرتين.**
- **Baseline قبل أي تعديل**: سكريبت تدقيق آلي جديد يمسح ملفات النواة التسعة ضد قائمة سلاسل الدومين (أسماء أحداث/وظائف/كيانات من البيانات + قائمة ثابتة احتياطية) ويمسح الكسور العشرية خارج أسطر `rules.get` — ويوثق **الجرد الكامل الفعلي** للمخالفات (اكتشاف أي مخالفات غير الموثقة أعلاه).
- **بعد الإصلاح**: نفس السكريبت يجب أن يعطي **صفرًا**. الـ delta بين اللوجين هو الدليل على اكتمال التطهير لا اكتفاؤه بالمخالفات المعروفة.

**معيار القبول الحاكم (Step 4):**
| الفحص | المعيار |
|---|---|
| `ScenarioTest.gd` | 5/5 + **SHA256 مطابق حرفيًا** لـ `2610ed24...` — أي اختلاف = FAIL فوري |
| التدقيق الآلي بعد الإصلاح | صفر مخالفات دومين + صفر كسور خارج `rules.get` في ملفات النواة |
| اختبارات Phase 7/8/9 | 21/21 + 14/14 + 14/14 من جديد |
| `validate_memory.py` | PASS |

**حدود صارمة:** ملفات النواة المتعددة: `Simulation.gd` فقط. `ScenarioTest.gd` لا يُلمس. لا توسع جانبي (ObjectDB leaks والأداء قوائم مستقلة). أي انحراف checksum = توقف فوري وتوثيق.

**اتثبت عمليًا (بعد التنفيذ):**

1. **Baseline Audit موثق أولًا**: 15 Gate violation في `Simulation.gd` (12 تحكمية + 3 أرقام حرفية) + 29 جرد مؤجل — شمل اكتشافات غير موثقة سابقًا: passthrough في `DecisionSystem.gd` وdefault `growth=0.02` في `WorldState`. Evidence: [phase10_audit_baseline.log](.ai/evidence/tests/phase10_audit_baseline.log)
2. **التنفيذ**: `data/rules/dispatch.json` + طبقة محتوى جديدة `scripts/game_event_handlers.gd` (كل منطق الدومين انتقل إليها) + `Simulation.gd` صار آلة dispatch عامة بصفر أسماء دومين، مع علم `one_shot` من البيانات بدل مقارنة الاسم، وتسجيل الوظائف الابتدائية من البيانات.
3. **Post Audit: CLEAN** — Gate = 0 على `Simulation.gd`؛ الجرد المؤجل (15 ملاحظة في DecisionSystem/WorldState) موثق في [`audit_report.md`](audit_report.md) المُعاد توليده كمهمة متابعة منفصلة.
4. **إثبات التكافؤ الصارم**: ScenarioTest **5/5 + SHA256 مطابق حرفيًا** (`a7cff9f1...`) = صفرية تغير السلوك رياضيًا. Evidence: [phase10_scenariotest.log](.ai/evidence/tests/phase10_scenariotest.log)
5. **لا انهيار جانبي**: Test 1 = 21/21، Test B = 14/14، Test C = 14/14 من جديد.

> **ملاحظة تصحيح أثناء التنفيذ (شفافية):** أول تشغيل بعد الـ refactor كشف خطأ تنفيذًا واحدًا (`_dispatch` لم يكن يُخزَّن ⇒ صفر وظائف مسجلة ⇒ Checksum FAIL). شُخص بمجس تشخيصي مؤقت، أُصلح بسطر واحد، وأُعيد التشغيل الكامل للوج نظيف.

---

## خارج نطاق هذا الملف (مؤجل عمدًا، متسجل في `04-اسئلة-تصميم-مفتوحة.md`)

- Military Institution Framework الكامل
- Character/Event Significance System
- Media/Information Graph
- عمق الاقتصاد
- Visual/No-Code Authoring Layer
- العالم البصري المستقل
- Generic Rule Runtime / DSL كامل (لحد ما تثبت الحاجة الفعلية في Phase 3)
- Lifecycle contract رسمي كامل (لحد ما يبقى عندنا أكتر من نظام يستهلكه)
- Headless executable منفصل (لحد ما تظهر حاجة فعلية زي CI)
- Derived Personality Traits (أرقام سمات مشتقة بصيغ مجمدة من حقائق بنية) — هي "الشخصيات الفعلية" المؤجلة بعد Phase 9؛ تُفتح فقط عند ظهور طبقة قرار مستهلكة لها، وتُختبر ساعتها بخصائص Monotonicity مسجلة مسبقًا

كل دول ما تتلمسش قبل ما Phase 5 هنا ينجح.