# Phase 7 — Test 1: Derived Importance
## مواصفة تسليم لتنفيذ برمجي (Implementation Handoff Spec)

**⚠️ للـ AI البرمجي المكلَّف بالتنفيذ:** نفّذ هذا الاختبار **فقط**. لا تُضِف Personality، لا GOAP، لا Diplomacy، لا أي نظام آخر من الخريطة الأشمل. لا تُعدِّل أي جزء من الـ Kernel المُثبَت في Phase 6 (Sleep/Wake, Event Queue, Scheduler, Determinism) — استخدمه كما هو. الهدف الوحيد لهذا الاختبار: **إثبات أو نفي فرضية واحدة محددة بدقة، عبر عدة عوالم مصغّرة متنوعة، لا عالم واحد مصمَّم عمدًا لينجح.**

### ⚠️ تحذير منهجي جوهري (اقرأه قبل أي سطر كود)

النسخة الأولى من هذا الاختبار كانت تحتوي عالمًا واحدًا فقط (الصين/تايوان/الرقائق). تم رفضها لسببين:
1. **عالم واحد مصمَّم يدويًا لا يثبت شيئًا** — يثبت أننا نعرف نجمع أرقامًا، لا أن القاعدة عامة (Invariant).
2. **معيار PASS/FAIL بقيم مطلقة (مثل 0.56) يختبر معادلتنا المختارة، لا الفرضية نفسها.** لو رقم دقيق فشل، لن نعرف هل الفرضية غلط أم اختيارنا لصيغة الحساب غلط.

لذلك هذا التسليم مبني على **اختبار خصائص (Properties)** لا قيم مطلقة: نتحقق من *اتجاه* التغيّر (يزيد/يقل/يبقى ثابت) لا من رقم بعينه. هذا أقوى علميًا ويفصل بين "الفرضية صحيحة" و"صيغتي الحسابية سيئة".

**ملاحظة على مصطلح "Blind"**: العوالم 5 و6 أدناه **Blind في الأسماء فقط**، لا في البنية — نحن نعرف مسبقًا شكل شبكة الاعتماد فيها. المصطلح الدقيق هو **Structural Blind Test**: يختبر هل نفس الكود يعمل بلا تخصيص لأي اسم، لا هل الكود "يفهم" أسماء دول حقيقية. هذا كافٍ تمامًا لهذه المرحلة، ولا حاجة لبلايند حقيقي (مصادر بيانات لم نصممها نحن) في Test 1.

### القاعدة الإلزامية: التوقع قبل التنفيذ

لكل عالم، جدول "التغيير → التوقع الاتجاهي" (أدناه) مكتوب **قبل** تشغيل أي كود. بعد التنفيذ، تُقارَن النتيجة الفعلية بالتوقع المكتوب مسبقًا صراحة عمود بعمود. **ممنوع تفسير النتيجة كمنطقية بعد ظهورها** — إما طابقت اتجاه التوقع المسجَّل مسبقًا أو لم تطابقه.

---

## الفرضية قيد الاختبار

> **هل يمكن للنظام أن يشتق "أهمية" مورد/قدرة معينة لدولة معينة من حالة العالم وشبكة الاعتماديات وحدها — بدون أن نكتب هذه الأهمية كقيمة مباشرة في أي مكان؟**

بصيغة الرفض الصريح: **ممنوع** وجود أي سطر كود أو بيانات من الشكل:
```
Taiwan.strategic_importance = 95        ← ممنوع
China.cares_about["Taiwan"] = true      ← ممنوع
China.goal = "control_taiwan"           ← ممنوع
```

المسموح فقط: حقائق محايدة عن القدرات والاعتماديات، والقيمة "تظهر" من معالجتها.

---

## العوالم الستة (كل عالم = مدخلات مختلفة، نفس الكود بالحرف)

### عالم 1 — الاعتماد المباشر البسيط (Baseline)
```
Countries: China, USA, Taiwan, Egypt, Turkey
Capabilities: semiconductors, shipping_route, military_power

Taiwan.produces[semiconductors] = 0.80
USA.produces[semiconductors]    = 0.15
Egypt.controls[shipping_route]  = 1.0

China.depends_on[semiconductors]  = 0.70
USA.depends_on[semiconductors]    = 0.20
Turkey.depends_on[semiconductors] = 0.05
Egypt.depends_on[semiconductors]  = 0.02
```
**جدول التوقع الاتجاهي (يُكتب قبل التشغيل — قيم اتجاهية لا أرقام مطلقة):**
| القياس | التوقع |
|---|---|
| derived_importance(China, Taiwan) مقارنة بـ derived_importance(Egypt, Taiwan) | **أعلى** (بغض النظر عن الرقم بالضبط) |
| derived_importance(China, Taiwan) مقارنة بـ derived_importance(Turkey, Taiwan) | **أعلى** |
| derived_importance(Egypt, Taiwan) | **قريبة من صفر نسبيًا** لباقي القيم في نفس العالم |

### عالم 1ب — اختبار السببية المباشرة (Isolated Variable Change، عالم فرعي جديد لعزل Monotonicity)
نفس عالم 1، لكن **تغيير متغير واحد بس كل مرة**، بلا أي تغيير آخر:
```
Run 1: China.depends_on[semiconductors] = 0.70  → derived_importance = X
Run 2: China.depends_on[semiconductors] = 0.85  (زيادة فقط) → derived_importance = Y
Run 3: China.depends_on[semiconductors] = 0.50  (نقصان فقط) → derived_importance = Z
```
**جدول التوقع (Monotonicity — خاصية لا رقم):**
| القياس | التوقع |
|---|---|
| Y مقارنة بـ X | Y > X (زيادة الاعتماد لا يمكن أن تقلل الأهمية) |
| Z مقارنة بـ X | Z < X (نقصان الاعتماد لا يمكن أن يزيد الأهمية) |

كرِّر نفس الإجراء لتغيير `Taiwan.produces[semiconductors]` بمعزل عن كل شيء آخر (زيادة الإنتاج ⇒ الأهمية لا تقل؛ نقصانه ⇒ لا تزيد).

### عالم 2 — ظهور مصدر بديل (يجب أن تنخفض الأهمية دون تغيير الصين نفسها)
نفس عالم 1، مع إضافة:
```
SouthKorea.produces[semiconductors] = 0.40   (منافس جديد ظهر)
```
**⚠️ ملزم بالتصميم**: صيغة `derived_importance` المُختارة **يجب** أن تتضمن عامل ندرة/بدائل (scarcity/substitutability factor)، لا الاعتماد على المُنتِج الواحد فقط. إن لم يتغيّر شيء عند إضافة SouthKorea، هذا **فشل تصميم في الصيغة نفسها**، لا فشل في الفرضية — ويجب تعديل الصيغة (لا الفرضية) والإعادة.
**جدول التوقع:**
| القياس | التوقع |
|---|---|
| derived_importance(China, Taiwan) في عالم 2 مقارنة بعالم 1 | **أقل** (رغم أن China.depends_on لم يتغيّر إطلاقًا) |

### عالم 3 — اختفاء المصدر البديل (يجب أن ترتفع الأهمية مجددًا)
نفس عالم 2، مع:
```
SouthKorea.produces[semiconductors] = 0.0   (كارثة/انهيار الصناعة هناك)
```
**جدول التوقع:**
| القياس | التوقع |
|---|---|
| derived_importance(China, Taiwan) | ترتفع لتقترب من قيمتها في عالم 1 مجددًا |

### عالم 4 — سلسلة اعتماد متعددة الخطوات (Dependency Chain)
```
Countries: China, USA, Taiwan
Capabilities: semiconductors, AI_capability, military_capability_modern

semiconductors → enables → AI_capability → enables → military_capability_modern
Taiwan.produces[semiconductors] = 0.80
China.depends_on[semiconductors] = 0.70
China.depends_on[AI_capability] = 0.65        (حقيقة استخدام قائمة، لا "أولوية" أو "نية")
```
**⚠️ تصحيح مهم**: النسخة السابقة من هذا العالم تضمَّنت `China.priority[military_capability_modern] = high`. تم حذفها لأنها **State Preference/Intent، لا World Fact** — نفس نوع التسريب الممنوع المذكور في الفرضية أعلاه (شبيه بكتابة `importance = 95` مباشرة، لكن بشكل مموَّه). Test 1 يشتق الأهمية من حقائق استخدام واعتماد قائمة فعلاً (`depends_on`)، لا من نوايا أو أولويات مُعلَنة مسبقًا. اشتقاق "لماذا الصين تهتم بالعسكرية أصلًا" مؤجَّل لاختبار لاحق (Test 3+ — Goal Emergence)، ممنوع هنا.

ثم **تغيير واحد فقط**: `China.domestic_semiconductor_capacity: 0 → 0.5`
**جدول التوقع:**
| القياس | التوقع |
|---|---|
| derived_importance(China, Taiwan) قبل التغيير | مرتفعة نسبيًا (السلسلة: رقائق → AI موجود اعتماد قائم عليه فعليًا) |
| derived_importance(China, Taiwan) بعد التغيير | **أقل** من قيمتها قبل التغيير، دون لمس أي قيمة أخرى غير `domestic_semiconductor_capacity` |

### عالم 5 — Structural Blind Test A (أسماء مجرّدة؛ Blind في الاسم لا في البنية)
```
Entities: Nation_Alpha, Nation_Beta, Nation_Gamma
Capabilities: Resource_X, Resource_Y

Nation_Beta.produces[Resource_X] = 0.85
Nation_Alpha.depends_on[Resource_X] = 0.75
Nation_Gamma.depends_on[Resource_X] = 0.01
```
**جدول التوقع:**
| القياس | التوقع |
|---|---|
| derived_importance(Nation_Alpha, Nation_Beta) مقارنة بـ derived_importance(Nation_Gamma, Nation_Beta) | Alpha أعلى بوضوح |

### عالم 6 — Structural Blind Test B (اعتماد متبادل/تنافسي)
```
Entities: Nation_Delta, Nation_Epsilon, Nation_Zeta
Capabilities: Resource_Z

Nation_Zeta.produces[Resource_Z] = 0.90
Nation_Delta.depends_on[Resource_Z]   = 0.60
Nation_Epsilon.depends_on[Resource_Z] = 0.60
```
ثم: `Nation_Delta.domestic_Resource_Z_capacity: 0 → 0.8` (دلتا بس اللي بنت بديل)
**جدول التوقع:**
| القياس | التوقع |
|---|---|
| derived_importance(Nation_Delta, Nation_Zeta) بعد التغيير مقارنة بقبله | **أقل بوضوح** |
| derived_importance(Nation_Epsilon, Nation_Zeta) بعد نفس التغيير مقارنة بقبله | **مطابقة تمامًا (صفر فرق)** — لأن التغيير خاص بدلتا فقط |

> **عالم 6 هو الاختبار الأخطر لـ Sub-test 1.3 (اللامبالاة)** — أي "تسرّب" في القيمة من دلتا إلى إبسيلون رغم عدم وجود سبب بياني يعني أن آلية الاشتقاق تحسب أوسع مما ينبغي، وهذا فشل معماري خطير يهدد مبدأ الأداء الأساسي عند التوسّع.

---

## الآلية المطلوب بناؤها (Minimal — لا أكثر)

**⚠️ الصيغة أدناه اقتراح عملي للبدء، لا الحقيقة المُختبَرة.** ما يُختبَر فعليًا هو الخصائص (Sub-tests 1.1–1.6)، لا مطابقة هذه الصيغة بالحرف. لو الصيغة أدناه فشلت في تحقيق خاصية معينة (مثل الاستجابة للبدائل في عالم 2)، **يُعدَّل شكل الصيغة** (مثلًا بإضافة عامل ندرة) لا أن يُعتبر الاختبار كله فاشلاً تلقائيًا — طالما التعديل ما زال محايدًا (بيانات مشتقة حسابيًا، لا قيمة أهمية مكتوبة يدويًا).

### دالة اشتقاق مقترحة للبدء:
```
derived_importance(observer_country, target_capability) =
    Σ over all paths from target_capability to observer's known priorities:
        (observer.depends_on[capability_on_path]) 
        × (path_criticality_weight)
```

**تبسيط أكبر مسموح للاختبار الأول**: لو السلسلة معقدة قوي، ينفع تبسيط الصيغة لـ:
```
derived_importance(China, Taiwan) = 
    Taiwan.produces[semiconductors] 
    × China.depends_on[semiconductors]
    × (scarcity_factor: 1 / عدد الدول اللي بتنتج نفس المورد بنسبة معتبرة)
```

النقطة الجوهرية مش شكل المعادلة بالضبط — النقطة إن **الناتج رقم محسوب**، مش رقم مكتوب.

---

## الاختبارات الفرعية (Sub-tests) ومعيار كل واحد

### Sub-test 1.1 — الاشتقاق الأساسي والمقارنة النسبية (عوالم 1، 5)
**PASS إذا**: الترتيب النسبي بين الكيانات طابق جدول التوقع الاتجاهي المكتوب مسبقًا (مين أعلى من مين)، والفرق ناتج حسابيًا من البيانات لا من كود خاص بكل كيان.
**FAIL إذا**: احتجنا أي شرط خاص مكتوب زي `if country == "China": importance += X`، أو خالف الترتيب النسبي التوقع.

### Sub-test 1.2 — Monotonicity (السببية المباشرة، عالم 1ب تحديدًا)
**PASS إذا**: زيادة `depends_on` أو `produces` بمعزل عن كل شيء آخر لا تُنقِص الأهمية أبدًا، ونقصانهما لا يزيدها أبدًا — بلا استثناء واحد عبر كل التجارب.
**FAIL إذا**: ظهر أي انعكاس اتجاه واحد (زيادة اعتماد أدَّت لنقصان أهمية أو العكس).

### Sub-test 1.3 — الاستبدالية ثنائية الاتجاه (عوالم 2، 3)
**PASS إذا**: القيمة تنخفض عند ظهور بديل، **وترتفع مجددًا** عند اختفاء البديل (عالم 3) — أي أن الحركة قابلة للعكس لا أحادية الاتجاه فقط.
**FAIL إذا**: القيمة لم تتحرك عند ظهور/اختفاء البديل، أو تحركت في اتجاه واحد غير قابل للعكس.

### Sub-test 1.4 — اللامبالاة الصحيحة (Isolation، عوالم 1، 5، 6 — الأهم في كل الاختبار)

> **⚠️ تعديل رسمي معتمد (2026-08-24، قرار صاحب المشروع بعد Run 3):** النص الأصلي أدناه محفوظ كما هو للسجل التاريخي، لكن **المعيار المعتمد الآن** هو:
> - بند "ثبات bitwise" ينطبق حصرًا على كيان **بلا أي اعتماد بياني مسجَّل فعليًا (zero-dependency)**: `dependency = 0 ⇒ importance = 0` بالضبط مهما تغيّر إنتاج الموردين في العالم.
> - كيان weakly-connected (اعتماد ضئيل لكنه ليس صفرًا، مثل مصر 0.02) **ليس** isolated؛ حركته النسبية مع تغيّر السوق هي نفس السلوك الذي يكافئه Sub-test 1.3 بـ PASS — لا تناقض نمذجي، والتناقض كان في نص هذا البند وحده.
> - الأساس: Run 3 أثبت الآلية PASS 4/4 ([phase7_test1_run3_zero_dep_control.log](file:///.ai/evidence/tests/phase7_test1_run3_zero_dep_control.log)) — `dependency=0 ⇒ importance=0.0` bitwise عبر ثلاث تجارب تغيّر إنتاج الموردين بنفس الدالة دون تعديل. الفشل الأصلي لهذا الـ Sub-test كان false negative من تصميم الفحص (اختيار كيان مرتبط كمثال على معزول)، لا من الصيغة ولا الـ kernel.

**PASS إذا**: أي كيان بلا اعتماد بياني مسجَّل يعطي قيمة قريبة من صفر نسبيًا **وتبقى ثابتة تمامًا** حتى لو تغيّر كيان آخر لا علاقة له به (عالم 6: تغيير Epsilon = صفر فرق بالضبط، لا "فرق صغير").
**FAIL إذا**: أي "تسرّب" قيمة، مهما كان ضئيلاً، لكيان غير مرتبط بيانيًا بالتغيير.

> **ملاحظة حرجة تبقى كما هي**: فشل هذا الـ Sub-test تحديدًا أخطر من فشل أي شيء آخر، لأنه يهدد مباشرة مبدأ الأداء الأساسي (O(الروابط ذات الصلة) لا O(كل الكيانات)) عند التوسّع لاحقًا.

### Sub-test 1.5 — الثبات عبر البنية (Structural Invariance) — عوالم 5، 6 تحديدًا
**PASS إذا**: **نفس دالة `derived_importance` بالحرف الواحد** (بلا أي `if world_id == ...` أو أي تخصيص باسم الكيان) أعطت نتائج صحيحة في العوالم المجرّدة الأسماء (5، 6) تمامًا كما في العوالم المسمّاة (1-4).
**FAIL إذا**: احتاج الكود أي تفريع خاص باسم كيان أو عالم معيّن ليعمل صح — هذا يعني أن ما نجح ليس قاعدة عامة بل حالة خاصة مموَّهة.

### Sub-test 1.6 — عدم القفز لأي قرار أو تسريب Preference
**PASS إذا**: (أ) المخرج في كل العوالم الستة هو قيمة رقمية (أو قائمة قيم) بلا أي منطق قرار مرفق — لا Goal، لا Action. (ب) كل المدخلات في كل عالم هي World Facts محايدة (إنتاج/اعتماد قائم فعليًا) بلا أي "نية" أو "أولوية" أو "تفضيل" مُعلَن مسبقًا لأي كيان.
**FAIL إذا**: ظهر أي سلوك من نوع "إذن الصين تقرر كذا" (هذا يخص Test 3/4 لاحقًا)، أو وُجد أي مدخل من نوع `priority[...] = high` أو ما يعادله.

---

## معيار النجاح الكلي للاختبار (Overall PASS/FAIL)

```
PASS الكلي = Sub-test 1.1 PASS (كل العوالم)
           AND Sub-test 1.2 PASS   ← Monotonicity
           AND Sub-test 1.3 PASS   ← Substitutability
           AND Sub-test 1.4 PASS   ← Isolation (الأخطر للأداء)
           AND Sub-test 1.5 PASS   ← Structural Invariance (الأهم لإثبات أن هذا ليس toy example)
           AND Sub-test 1.6 PASS   ← لا قرار، لا تسريب Preference
```

لو فشل Sub-test واحد في **عالم واحد بس** من الستة، الاختبار الكلي **FAIL** — ولازم يُوثَّق بالتحديد أي عالم وأي قياس فشل وليه، مش نحاول "نصلحه بسرعة" ونكمل. الفشل الجزئي هنا معلومة مهمة، مش عائق يتجاوز. **نجاح 5 عوالم من 6 لا يعني PASS جزئي — يعني أن القاعدة غير عامة، وهذا بالضبط ما كنا نخشاه.**

**استثناء واحد مسموح**: لو فشل Sub-test 1.3 تحديدًا (الاستبدالية) بسبب أن الصيغة المقترحة لا تتضمن عامل ندرة، فهذا **فشل في اختيار الصيغة، لا في الفرضية** — يُعاد تصميم شكل المعادلة (مع الحفاظ على كل القيود الأخرى) وإعادة تشغيل كل العوالم الستة من جديد، لا نُسجِّل "FAIL نهائي" من أول محاولة.

---

## حدود صارمة على التنفيذ (لتفادي الانزلاق لمشروع أكبر)

1. **لا استخدام لـ ECS ولا GDExtension ولا C++.** GDScript بسيط + Dictionary/Array، بالضبط زي فلسفة الـ Prototype الأول في Phase 6.
2. **لا واجهة رسومية.** طباعة نتائج نصية (print/Debug UI بسيط زي Phase 9 في الخطة الأصلية) كافية تمامًا.
3. **لا إضافة كيانات أو عوالم فوق الستة المذكورة.** لو النموذج نجح، التوسّع (200 دولة) يكون اختبار منفصل (Test 10 — Scale) مش جزء من هذا التسليم.
4. **حجم الكود المتوقع**: لو تجاوز هذا الاختبار 400-500 سطر GDScript (زاد الحد قليلًا عن النسخة الأولى بسبب تعدد العوالم، لكن المنطق الأساسي يفضل نفسه)، هذا مؤشر تحذير أننا بنبني أكتر من اللازم — يُراجَع النطاق قبل الاستمرار.
5. **إلزامي**: بيانات كل عالم من الستة تُخزَّن كملفات/بنى بيانات منفصلة تمامًا (مثال: `world_1.json` … `world_6.json` أو ما يعادلها)، وتُغذَّى لنفس دالة `derived_importance` دون أي تفرّع بالاسم. هذا هو الدليل العملي على Sub-test 1.4.

---

## المخرج المطلوب من الـ AI البرمجي بعد التنفيذ

1. الكود نفسه (ملف أو ملفين GDScript).
2. جدول نتائج: لكل Sub-test ولكل عالم، القيم المحسوبة الفعلية (أرقام حقيقية، حتى لو المعيار اتجاهي) + PASS/FAIL.
3. لو أي Sub-test فشل: وصف دقيق لسبب الفشل من واقع الكود، لا تخمين. **يُذكَر صراحة**: هل الفشل يشير لمشكلة في الفرضية نفسها، أم في الصيغة الرياضية المختارة (انظر الاستثناء أعلاه)؟
4. **بدون أي اقتراحات "الخطوة الجاية"** — القرار بشأن الانتقال لـ Test 2 أو تعديل Test 1 يُتَّخذ من قِبل صاحب المشروع (أحمد) والمراجع (Claude)، لا من منفّذ الكود.
